# 🔧 Trophy Fishing - 技术选型文档

> **版本**：v1.0  
> **阶段**：技术选型  
> **前置依赖**：`@concept.md`, `@systems-breakdown.md`  
> **生成者**：game-tech-stack-planner

---

## 一、技术约束

### 1.1 平台锁定

| 维度 | 选择 | 原因 |
|------|------|------|
| **目标平台** | UEFN (Fortnite Creative) | 用户明确要求 |
| **跨平台支持** | 由 Fortnite 引擎托管 | 无需自行处理 |
| **联机支持** | 由 Fortnite 服务托管 | 无需自行处理 |
| **发布渠道** | Fortnite Creative 地图 | 唯一选项 |

### 1.2 UEFN 技术限制

| 限制项 | 描述 | 应对策略 |
|--------|------|----------|
| 语言 | 仅支持 Verse | 使用 Verse 开发 |
| 引擎版本 | 锁定 UEFN 版本 | 跟随 Epic 更新 |
| 资源限制 | 内存/对象数有上限 | 精简资源使用 |
| 持久化 | 仅 Player 级别存储 | 使用 persistable |
| UI | 限于 Creative Devices | 使用 Widget 系统 |

---

## 二、技术栈

### 2.1 核心技术

```
┌─────────────────────────────────────────────────┐
│                   UEFN                          │
├─────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐               │
│  │   Verse     │  │   Devices   │               │
│  │  (逻辑层)   │  │  (交互层)   │               │
│  └─────────────┘  └─────────────┘               │
│         │                │                      │
│         ▼                ▼                      │
│  ┌─────────────────────────────────┐           │
│  │        Creative Devices          │           │
│  │  (Button, Trigger, Prop, etc.)  │           │
│  └─────────────────────────────────┘           │
│                    │                            │
│                    ▼                            │
│  ┌─────────────────────────────────┐           │
│  │        Fortnite Runtime          │           │
│  │  (Physics, Rendering, Network)  │           │
│  └─────────────────────────────────┘           │
└─────────────────────────────────────────────────┘
```

### 2.2 技术组件清单

| 组件 | 技术 | 用途 |
|------|------|------|
| **游戏逻辑** | Verse | 状态机、计算、流程控制 |
| **交互触发** | Trigger Device | 钓点交互检测 |
| **UI 显示** | HUD Message Device | 文字提示、收益显示 |
| **3D 展示** | Creative Prop | 鱼模型、展示台 |
| **音效** | Audio Player Device | 钓鱼音效、反馈 |
| **粒子效果** | VFX Device | 水花、稀有光效 |
| **数据持久化** | persistable 属性 | 玩家存档 |
| **计时器** | Verse Sleep/Timer | 钓鱼等待、收益累积 |

---

## 三、Verse 架构设计

### 3.1 模块划分

```
project/
├── devices/
│   ├── fishing_spot_device.verse      # 钓点交互
│   ├── display_slot_device.verse      # 展示位
│   ├── income_manager_device.verse    # 收益管理
│   └── upgrade_shop_device.verse      # 升级商店
├── managers/
│   ├── game_manager.verse             # 游戏状态
│   ├── player_data_manager.verse      # 玩家数据
│   └── fish_database.verse            # 鱼种数据
├── systems/
│   ├── fishing_system.verse           # 钓鱼逻辑
│   ├── trophy_system.verse            # 战利品逻辑
│   └── economy_system.verse           # 经济逻辑
└── common/
    ├── types.verse                    # 数据类型定义
    ├── constants.verse                # 常量配置
    └── utils.verse                    # 工具函数
```

### 3.2 核心类型定义

```verse
# types.verse

rarity_tier := enum:
    Common
    Uncommon
    Rare
    Epic

fish_data := struct<persistable>:
    Id : string
    Name : string
    Rarity : rarity_tier
    BaseIncome : int

player_save_data := struct<persistable>:
    Gold : int
    RodLevel : int
    Inventory : [string]int           # FishId -> Quantity
    Collection : [string]logic        # FishId -> Discovered
    DisplaySlots : [int]?string       # SlotId -> FishId
    LastExitTime : float
```

### 3.3 设备通信模式

```verse
# 事件驱动通信
fishing_events := module:
    OnFishCaught : event(fish_data) = event(fish_data){}
    OnFishPlaced : event(int, fish_data) = event(int, fish_data){}
    OnGoldChanged : event(int) = event(int){}
    OnLevelUp : event(int) = event(int){}

# 使用示例
fishing_spot_device := class(creative_device):
    OnFishingSuccess(Fish : fish_data) : void =
        fishing_events.OnFishCaught.Signal(Fish)

trophy_system := class(creative_device):
    OnBegin<override>()<suspends> : void =
        fishing_events.OnFishCaught.Subscribe(HandleFishCaught)
```

---

## 四、Creative Devices 选型

### 4.1 必需 Devices

| Device | 用途 | 数量 |
|--------|------|------|
| Button Device | 钓点交互触发 | 1 |
| Prop Manipulator | 鱼模型显示 | 3 (展示位) |
| Audio Player Device | 音效播放 | 1 |
| Billboard Device | UI 文字显示 | 多个 |
| Timer Device | 钓鱼计时（备用） | 1 |

### 4.2 可选 Devices

| Device | 用途 | 优先级 |
|--------|------|--------|
| VFX Spawner Device | 水花/光效 | P1 |
| Camera Device | 钓鱼特写 | P2 |
| Cinematic Sequence | 首获动画 | P3 |

---

## 五、数据持久化方案

### 5.1 存储结构

```verse
# 使用 UEFN 的 persistable 系统

player_persistence := class<persistable>:
    # 核心数据
    var Gold : int = 0
    var RodLevel : int = 1
    
    # 背包数据 (简化为字符串序列化)
    var InventoryData : string = ""
    
    # 图鉴数据
    var DiscoveredFish : string = ""
    
    # 展示位数据
    var Slot1Fish : string = ""
    var Slot2Fish : string = ""
    var Slot3Fish : string = ""
    
    # 时间戳
    var LastPlayTime : float = 0.0
```

### 5.2 存档时机

| 事件 | 存档内容 |
|------|----------|
| 钓到鱼 | Inventory, Collection |
| 放置鱼 | DisplaySlots, Inventory |
| 升级 | RodLevel, Gold |
| 领取收益 | Gold |
| 退出游戏 | LastPlayTime |

---

## 六、性能考量

### 6.1 资源预算

| 资源类型 | 预算 | 实际使用 | 备注 |
|----------|------|----------|------|
| Verse 设备 | 50 | ~10 | 余量充足 |
| Prop 对象 | 100 | ~10 | 鱼+展示台 |
| 音效 | 20 | ~8 | 钓鱼反馈 |
| 粒子 | 10 | ~3 | 水花/光效 |

### 6.2 优化策略

| 策略 | 描述 |
|------|------|
| 对象池 | 鱼模型复用，不频繁创建销毁 |
| 延迟加载 | 图鉴数据仅在打开时加载 |
| 定时器合并 | 使用单一主循环而非多计时器 |
| UI 懒更新 | 仅在数据变化时刷新 UI |

---

## 七、开发工具链

### 7.1 必需工具

| 工具 | 版本 | 用途 |
|------|------|------|
| UEFN | 最新稳定版 | 开发环境 |
| Verse | UEFN 内置 | 编程语言 |
| Fortnite | 最新版 | 测试运行 |

### 7.2 推荐工具

| 工具 | 用途 |
|------|------|
| VS Code + Verse 插件 | 代码编辑 |
| Blender | 3D 模型制作（如需自定义） |
| Audacity | 音效编辑 |
| Figma | UI 原型设计 |

### 7.3 版本控制

```
推荐：Git + GitHub/GitLab

.gitignore 建议:
  - Saved/
  - Intermediate/
  - DerivedDataCache/
  - *.log
```

---

## 八、风险评估

### 8.1 技术风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| Verse 功能限制 | 中 | 高 | 提前验证关键功能 |
| UEFN 版本更新 | 中 | 中 | 定期同步更新 |
| 持久化数据丢失 | 低 | 高 | 多重存档检查 |
| 性能不足 | 低 | 中 | 预留优化空间 |

### 8.2 技术验证清单

**MVP 前必须验证**：

- [ ] Verse 状态机实现可行性
- [ ] persistable 跨会话存储正常
- [ ] Prop 动态显示/隐藏
- [ ] 定时器精度满足需求
- [ ] UI 文字动态更新

---

## 九、里程碑技术目标

### 9.1 M0 原型 (3-5 天)

| 目标 | 验证内容 |
|------|----------|
| 基础钓鱼流程 | 状态机、交互、计时 |
| 单一展示位 | Prop 显示 |
| 简单 UI | 金币显示 |

### 9.2 M1 MVP (2 周)

| 目标 | 技术实现 |
|------|----------|
| 完整 5 系统 | 全部 Verse 逻辑 |
| 3 展示位 | 多 Prop 管理 |
| 数据持久化 | persistable 存档 |
| 离线收益 | 时间戳计算 |

---

## 十、参考资源

### 10.1 官方文档

- [Verse Language Reference](https://dev.epicgames.com/documentation/en-us/uefn/verse-language-reference)
- [UEFN Documentation](https://dev.epicgames.com/documentation/en-us/uefn)
- [Creative Devices](https://dev.epicgames.com/documentation/en-us/uefn/devices-in-unreal-editor-for-fortnite)

### 10.2 示例项目

- UEFN Sample Projects (Epic 官方)
- Fortnite Creative Showcase Maps

---

## 十一、下一步

**技术选型完成** ✅

技术栈确认：
- **引擎**：UEFN
- **语言**：Verse
- **平台**：Fortnite Creative
- **持久化**：persistable 属性

下一阶段：调用 `game-implementation-planner` 生成实施计划

---

*文档生成时间：2025-12-26*
