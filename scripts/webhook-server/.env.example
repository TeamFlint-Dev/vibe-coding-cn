# GitHub Webhook Hub 环境变量
# 请填写你的实际值

# ==================== 必需配置 ====================

# GitHub Personal Access Token (用于 repository_dispatch 等系统操作)
# 需要权限: repo (或至少 public_repo)
GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxx

# ==================== 多账号配置 (Copilot 修复请求) ====================

# 用户账号 PAT 列表 (用于以用户身份发表 @copilot 评论)
# 格式: username1:pat1,username2:pat2,...
# 注意:
#   - 必须是真人用户账号的 PAT，不能是 bot 账号
#   - GitHub Copilot Agent 只会响应真人用户的 @copilot 提及
#   - 系统会自动轮换账号以分摊 Copilot 额度消耗
#   - 每个账号连续 3 次额度错误后会被暂时禁用
GITHUB_USER_PATS=user1:ghp_xxx,user2:ghp_yyy

# ==================== 安全配置 ====================

# Webhook Secret (与 GitHub Webhook 配置中的 Secret 一致)
WEBHOOK_SECRET=your-webhook-secret-here

# Callback Secret (Actions 回调时的签名验证)
# 与 GitHub Actions Secrets 中的 CALLBACK_SECRET 一致
CALLBACK_SECRET=your-callback-secret-here

# ==================== 业务配置 ====================

# 最大重试次数 (超过后 @人类介入)
MAX_RETRY_COUNT=5

# 需要 @ 的人类用户名 (用于人工介入通知)
NOTIFY_USER=Maybank01

# ==================== 仓库配置 ====================

REPO_OWNER=TeamFlint-Dev
REPO_NAME=vibe-coding-cn

# ==================== 服务配置 ====================

PORT=8080

# ==================================================
# GitHub Actions Secrets 配置（在 GitHub 仓库中设置）
# ==================================================
# 
# 需要在 GitHub 仓库 Settings → Secrets and variables → Actions 中添加：
#
# | Secret 名称      | 值                                          | 说明                     |
# |------------------|---------------------------------------------|-------------------------|
# | CALLBACK_URL     | http://<服务器IP>:<PORT>/callback            | Hub 回调地址             |
# | CALLBACK_SECRET  | <与上面 CALLBACK_SECRET 一致>                | 回调签名密钥             |
# | RUNNER_PROXY     | http://127.0.0.1:10808                       | Self-hosted runner 代理  |
#
# 注意：RUNNER_PROXY 用于 self-hosted runner 访问 GitHub API
# 如果 runner 网络良好（如云服务器），可以不配置
