# Frontmatter 代码片段

> **用途**: 工作流 YAML 配置模板  
> **来源**: workflowAuthoring Skill

---

## 1. 最小 Frontmatter

```yaml
---
on: workflow_dispatch
permissions:
  contents: read
---
```

---

## 2. Slash Command 触发

```yaml
---
on:
  slash_command:
    name: commandname
---
```

---

## 3. 多事件 Slash Command

```yaml
---
on:
  slash_command:
    name: commandname
    events: [issue_comment, discussion_comment]
---
```

---

## 4. 事件驱动触发

```yaml
---
on:
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened, synchronize]
---
```

---

## 5. 定时触发

```yaml
---
on:
  daily:  # 简化语法
---
```

```yaml
---
on:
  weekly: tuesday  # 每周二
---
```

```yaml
---
on:
  schedule:
    - cron: '0 0 * * 1'  # 每周一 00:00
---
```

---

## 6. 模糊定时

```yaml
---
on:
  weekdays:  # 工作日
---
```

---

## 7. Workflow Run 触发

```yaml
---
on:
  workflow_run:
    workflows: [workflow-name]
    types: [completed]
---
```

---

## 8. 完整 Frontmatter 模板

```yaml
---
id: workflow-id              # 可选：自定义工作流 ID
description: "Short desc"    # 描述
model: claude-sonnet         # 模型选择

on:
  slash_command:
    name: mycommand
    events: [issue_comment, discussion_comment]

permissions:
  contents: read
  issues: write

tools:
  repo-memory:
    branch-name: memory/default
    file-glob: "**"
  github:
    toolsets: [default]
  bash: [":*"]
  edit:

imports:
  - shared/mcp/context7.md
  - shared/rules/security.md

safe-outputs:
  create-issue:
    max: 5
    title-prefix: "[auto] "
    labels: [ai-generated]
  add-comment:
    max: 10

network:
  allowed:
    - node
    - python

messages:
  run-started: "🚀 Starting..."
  run-success: "✅ Completed!"
  run-failure: "❌ Failed!"
  footer: "> Generated by [{workflow_name}]({run_url})"
---
```

---

## 9. 权限配置参考

```yaml
# 只读权限（推荐默认）
permissions:
  contents: read
  issues: read
  pull-requests: read
```

```yaml
# 写权限（谨慎使用）
permissions:
  contents: write  # 创建 PR
  issues: write    # 创建/更新 Issue
  pull-requests: write
```

```yaml
# Actions 权限（Meta-Orchestrator）
permissions:
  actions: read    # 查询工作流运行
```

---

## 10. 工具配置参考

```yaml
# 基础工具
tools:
  bash: [":*"]     # 所有 bash 命令
  edit:            # 文件编辑

# GitHub 工具
tools:
  github:
    toolsets: [default]  # 基础 GitHub 工具
    
tools:
  github:
    toolsets: [default, actions]  # 包含 actions 查询

# MCP 工具
tools:
  serena: ["go"]   # 带参数的 MCP 服务器
```

```yaml
# Repo-memory 工具
tools:
  repo-memory:
    branch-name: memory/default
    file-glob: "**"
    
tools:
  repo-memory:
    branch-name: memory/my-workflow
    file-glob: "**/*.json"  # 只读 JSON 文件
```
