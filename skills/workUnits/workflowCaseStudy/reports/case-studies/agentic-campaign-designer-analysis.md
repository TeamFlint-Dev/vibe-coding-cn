# agentic-campaign-designer 案例分析

> **分析日期**: 2026-01-09  
> **分析者**: workflow-case-study Agent (运行 #22)  
> **源文件**: `.github/agents/agentic-campaign-designer.agent.md` (gh-aw)  
> **commit**: 4cef998 (2026-01-09 重构)

---

## 📋 研究动机

**为什么选择这个工作流？**

1. **主题匹配度 80 分**（P1 主题：Agent 协作模式）
   - 展现了**双阶段协作架构**的设计演进
   - Phase 1 (campaign-generator) 设计，Phase 2 (本 Agent) 编译
   - 职责分离清晰，解决了环境限制问题

2. **Skill 空白度高**
   - 我们还没有深入研究"双阶段 Agent 协作"模式
   - 这种架构在 #9450 刚刚重构优化过
   - 可以填补"Agent 编排"知识空白

3. **模式新颖度高**
   - 不是传统的"单 Agent 完成所有任务"
   - 而是"设计 Agent + 执行 Agent"的**流水线式协作**
   - 每个阶段有明确的输入输出契约

4. **实用价值高**
   - 可迁移到我们的场景（如 Verse 代码生成 + 编译验证）
   - 解决了"不同阶段需要不同环境"的通用问题
   - 演示了如何在 Issue → Agent Session 之间传递状态

---

## 📊 概览

| 属性 | 值 |
|------|-----|
| **触发方式** | Copilot Coding Agent Session (手动调用) |
| **角色定位** | Phase 2 Compiler（阶段 2 编译器） |
| **前置依赖** | Phase 1 (campaign-generator.md) 已完成 |
| **核心职责** | 编译 campaign 规范 → 提交文件 → 创建 PR |
| **复杂度** | ⭐⭐⭐⭐ High（流程简单，但协作设计复杂） |

---

## 🎯 第一印象（30 秒扫描）

**这个工作流在解决什么问题？**

> Campaign 创建流程原本是单一工作流，但遇到了**环境限制问题**：
> - GitHub Actions runner 没有 `gh aw` CLI
> - Copilot Coding Agent session 有 CLI，但不适合执行长任务

**解决方案：双阶段架构**
- **Phase 1 (Actions)**: 设计、发现、生成规范文件（不需要 CLI）
- **Phase 2 (Agent)**: 编译、提交、创建 PR（需要 CLI）

**"用户"是谁？**

直接用户：Copilot Coding Agent  
间接用户：创建 campaign 的开发者  
协作伙伴：campaign-generator.md (Phase 1)

**文件长度暗示**

只有 196 行（简洁），因为：
- 职责单一（只负责编译和 PR）
- 不需要设计逻辑（Phase 1 完成）
- 接口明确（清晰的输入输出）

---

## 🔬 带着问题分析

### 研究问题 1：双阶段架构的边界如何划分？

**Phase 1 的职责**：
```markdown
- ✅ 创建 Project board
- ✅ 发现工作流（使用 catalog）
- ✅ 生成 .campaign.md 规范文件
- ✅ 写入仓库
- ✅ 更新 Issue
```

**Phase 2 的职责**：
```markdown
- ⏳ 编译 campaign (`gh aw compile`)
- ⏳ 提交文件 (spec + generated)
- ⏳ 创建 PR
```

**边界原则**：
- **按环境能力划分**：需要 CLI → Phase 2；不需要 → Phase 1
- **按任务性质划分**：设计/探索 → Phase 1；编译/执行 → Phase 2
- **按时间特性划分**：可能长时间运行 → Phase 1；快速完成 → Phase 2

**设计智慧**：
> 不是简单地"拆分任务"，而是**根据环境约束优化流程**。
> Phase 1 做尽可能多的工作，Phase 2 只做必须在特定环境的事。

---

### 研究问题 2：状态如何在两个阶段之间传递？

**传递机制**：

| 传递方式 | 内容 | 载体 |
|---------|------|------|
| **文件系统** | `.campaign.md` 规范文件 | Git 仓库 |
| **Issue 上下文** | Campaign ID, Project URL, Issue 号 | GitHub API |
| **显式参数** | 调用 Phase 2 时传入的上下文变量 | Copilot 参数 |

**关键设计**：
```markdown
You will be invoked with context containing:
- **Campaign ID**: `<campaign-id>`
- **Campaign file path**: `.github/workflows/<campaign-id>.campaign.md`
- **Project URL**: `<project-url>`
- **Issue number**: `<issue-number>`
```

**智慧点**：
- ✅ 不依赖隐式状态（如内存、缓存）
- ✅ 所有状态都在文件系统或 GitHub 中持久化
- ✅ Phase 2 可以独立运行（只要文件存在）
- ✅ 可追溯性强（所有状态都在 Git 历史中）

---

### 研究问题 3：如何处理 Phase 2 的失败？

**失败场景识别**：

```markdown
If the file doesn't exist, report to the issue and stop.
```

```markdown
**If compilation fails:**
- Review error messages
- Fix syntax issues in campaign file frontmatter
- Re-compile until successful
- Report errors to the issue if you can't fix them
```

**失败处理策略**：

| 失败类型 | 处理方式 | 回退策略 |
|---------|---------|---------|
| **文件不存在** | 报告到 Issue，停止 | 需要人工检查 Phase 1 |
| **编译错误（可修复）** | 自动修复，重新编译 | 迭代直到成功 |
| **编译错误（不可修复）** | 报告到 Issue | 需要人工介入 |

**智慧点**：
- ✅ **预期失败，而非假设成功**
- ✅ 明确区分"可自动修复"和"需人工介入"
- ✅ 所有失败都报告回 Issue（保持可观测性）

---

## 🏗️ 设计模式识别

### ✅ 已识别模式

#### 1. **Two-Phase Collaboration Pattern** ⭐⭐⭐⭐⭐⭐⭐⭐（新模式！）

**定义**：将复杂任务拆分为两个独立阶段，每个阶段由不同的 Agent/环境执行，通过文件系统和 GitHub API 传递状态。

**识别特征**：
- Phase 1 在一个环境（如 GitHub Actions）运行
- Phase 2 在另一个环境（如 Copilot Session）运行
- 明确的阶段分界点（文件写入仓库）
- 显式的状态传递契约

**适用场景**：
- 不同阶段需要不同的工具/环境
- 长任务 + 短任务的组合（避免超时）
- 设计阶段 vs 执行阶段分离

**优势**：
- ✅ 绕过环境限制（Phase 1 无 CLI，Phase 2 有）
- ✅ 职责清晰，易于维护
- ✅ 可独立测试和调试
- ✅ 失败隔离（Phase 1 失败不影响 Phase 2）

**挑战**：
- ❌ 状态传递复杂度增加
- ❌ 需要两次运行（端到端时间可能更长）
- ❌ 调试跨阶段问题困难

**代码示例**（来源：agentic-campaign-designer）：

```markdown
## Why This Separation?

- `gh aw compile` requires the gh-aw CLI binary
- CLI only available in Copilot Coding Agent sessions
- GitHub Actions runners don't have gh-aw CLI access
- Phase 1 does all design work; Phase 2 only compiles
```

**已知使用案例**：
- campaign-generator (Phase 1) + agentic-campaign-designer (Phase 2)

---

#### 2. **Explicit State Contract Pattern** ⭐⭐⭐⭐⭐⭐

**定义**：在工作流开头显式声明输入参数的契约，确保调用者清楚传递什么。

**识别特征**：
```markdown
You will be invoked with context containing:
- **Campaign ID**: `<campaign-id>`
- **Campaign file path**: `.github/workflows/<campaign-id>.campaign.md`
```

**优势**：
- ✅ 接口清晰，减少误用
- ✅ 便于单元测试（知道需要 mock 什么）
- ✅ 文档即代码（契约就是文档）

---

#### 3. **Verify-Before-Execute Pattern** ⭐⭐⭐⭐⭐

**定义**：在执行主要逻辑前，先验证前置条件是否满足。

**识别特征**：
```markdown
### Step 1: Verify Campaign File Exists

Check that the campaign file exists:

```bash
ls -la .github/workflows/<campaign-id>.campaign.md
```

If the file doesn't exist, report to the issue and stop.
```

**优势**：
- ✅ 早期失败（Fail Fast）
- ✅ 提供清晰的错误信息
- ✅ 避免部分执行导致的不一致状态

---

#### 4. **Progressive Error Recovery Pattern** ⭐⭐⭐⭐⭐⭐

**定义**：根据错误的可修复性，采用不同的恢复策略。

**识别特征**：
```markdown
**If compilation fails:**
- Review error messages
- Fix syntax issues in campaign file frontmatter
- Re-compile until successful
- Report errors to the issue if you can't fix them
```

**策略层级**：
1. **自动修复** → 继续执行
2. **重试** → 迭代直到成功
3. **报告并停止** → 需要人工介入

**优势**：
- ✅ 最大化自动化率
- ✅ 减少人工介入次数
- ✅ 保持系统稳定性（不会无限重试）

---

#### 5. **What You DON'T Do Pattern** ⭐⭐⭐⭐⭐⭐⭐

**定义**：显式声明工作流**不做什么**，避免职责蔓延。

**识别特征**：
```markdown
## What You DON'T Do

**You do NOT:**
- ❌ Parse issue requirements (already done in Phase 1)
- ❌ Discover workflows (already done in Phase 1)
...

**You ONLY:**
- ✅ Compile the already-created campaign file
- ✅ Commit the files
- ✅ Create a pull request
```

**设计智慧**：
- ✅ 防止功能蔓延（scope creep）
- ✅ 明确边界，便于维护
- ✅ 避免重复工作（Phase 1 已做的不再做）
- ✅ 提高代码审查效率（审查者清楚职责边界）

**对比**：
- ❌ **隐式边界**："这个工作流负责 campaign 创建" → 容易蔓延
- ✅ **显式边界**："只编译，不设计" → 清晰限制

---

#### 6. **Contextual Reporting Pattern** ⭐⭐⭐⭐

**定义**：在不同的执行阶段，向 Issue 报告不同粒度的进度信息。

**识别特征**：
```markdown
### Step 5: Report Success

Post a comment to issue #<issue-number>:

✅ **Campaign Compiled and PR Created!**
...
**Total time:** ~2-3 minutes (60% faster than old flow!)
```

**报告内容分层**：
- ✅ 状态（成功/失败）
- ✅ 关键指标（时间、文件数）
- ✅ 下一步行动（Review PR, Merge）
- ✅ 性能对比（与旧流程对比）

**优势**：
- ✅ 用户可见性高
- ✅ 数据驱动改进（知道优化效果）
- ✅ 引导用户行为（告诉下一步做什么）

---

## 🔧 Frontmatter 配置分析

| 配置项 | 值 | 设计意图探索 | 可迁移性 |
|-------|-----|------------|---------|
| `description` | "Compiles campaign specifications and creates pull requests (Phase 2 of optimized campaign creation flow)" | 清晰标注这是 Phase 2，说明是协作的一部分 | ✅ 高 - 多阶段任务都应标注 |
| `infer: false` | 禁用自动推断 | 确保行为可预测，不依赖隐式逻辑 | ✅ 高 - 编译/执行阶段应禁用推断 |

**观察**：
- Frontmatter 极简（只有 2 个字段）
- 这是一个 `.agent.md` 文件，而非 `.md` 工作流
- Agent 文件更关注**角色和职责**，而非触发器配置

**设计对比**：

| 类型 | 用途 | Frontmatter 重点 |
|------|------|----------------|
| `.md` 工作流 | 自动化任务 | `on`, `permissions`, `tools` |
| `.agent.md` Agent | Copilot 调用 | `description`, 行为约束 |

---

## 💻 Prompt 设计分析

### 结构层级

```
# Campaign Compiler (Phase 2)
├── Context (背景说明)
│   ├── Phase 1 已完成的工作
│   └── Phase 2 的任务
├── Why This Separation? (设计理由)
├── Your Task (执行步骤)
│   ├── Step 1: Verify
│   ├── Step 2: Compile
│   ├── Step 3: Commit
│   ├── Step 4: Create PR
│   └── Step 5: Report
├── What You DON'T Do (边界声明)
└── Reference Commands (工具参考)
```

**层级清晰度**: ⭐⭐⭐⭐⭐（5 星）

**观察**：
- 开头先说明**为什么分阶段**（设计理由）
- 然后说明**你只做什么**（职责边界）
- 最后给出**具体步骤**（操作指南）

**设计智慧**：
> 不是直接丢给 Agent 一堆步骤，而是先**建立上下文**，让 Agent 理解"为什么这样设计"。

---

### Prompt 节奏

| 部分 | 行数占比 | 节奏感受 |
|------|---------|---------|
| Context | 15% | 快速建立背景 |
| Why Separation | 10% | 强调设计理由 |
| Your Task | 50% | 详细操作指南 |
| What DON'T Do | 15% | 明确边界 |
| Reference | 10% | 工具速查 |

**节奏特点**：
- 前 25% 是"理解阶段"（为什么、什么）
- 中间 50% 是"执行阶段"（怎么做）
- 后 25% 是"边界和参考"（不做什么、工具）

**对比其他工作流**：
- 很多工作流直接进入"怎么做"
- 这个工作流花了 1/4 篇幅解释"为什么"
- 体现了对**Agent 认知负担**的重视

---

### 关键指令密度

**Step 2: Compile the Campaign** 部分：

```markdown
Run the gh-aw compile command:

```bash
gh aw compile <campaign-id>
```

This generates:
- `.github/workflows/<campaign-id>.campaign.g.md` (orchestrator)
- `.github/workflows/<campaign-id>.campaign.lock.yml` (compiled workflow)

**If compilation fails:**
- Review error messages
- Fix syntax issues in campaign file frontmatter
- Re-compile until successful
- Report errors to the issue if you can't fix them
```

**密度分析**：
- ✅ **先给命令**（明确动作）
- ✅ **再说产物**（预期结果）
- ✅ **最后说失败处理**（异常路径）

**设计原则**：**正常路径 → 预期结果 → 异常路径**

---

## 🔍 逆向工程设计意图

### 观察 1：为什么不在 Phase 1 就编译？

**表面原因**（文档明确说明）：
> `gh aw compile` requires the gh-aw CLI binary  
> CLI only available in Copilot Coding Agent sessions

**深层原因**（推断）：
1. **环境隔离**：Actions 和 Agent Session 的工具链不同
2. **职责分离**：设计（Phase 1）vs 编译（Phase 2）
3. **失败隔离**：编译失败不影响规范文件生成

**如果不这样设计会怎样？**
- ❌ Phase 1 需要安装 CLI（增加复杂度）
- ❌ 编译失败会阻塞整个流程
- ❌ 无法利用 Copilot Session 的交互能力

**设计权衡**：
- 增加了状态传递复杂度
- 但换来了环境解耦和更好的容错性

---

### 观察 2：PR 模板为什么这么详细？

**PR 模板包含**：
- 目的、Campaign 详情、工作流列表
- 文件清单、下一步、链接
- 生成方式说明

**设计意图推断**：

| 详细程度 | 目的 |
|---------|------|
| **非常详细** | 这是自动化生成的 PR，需要给审查者**完整上下文** |
| **包含下一步** | 引导用户行为（Review → Approve → Merge） |
| **包含生成方式** | 可追溯性（知道这是两阶段流程生成的） |
| **包含时间指标** | 展示优化效果（"60% faster than old flow"） |

**对比隐式设计**：
- 简单的 PR 只说"Add campaign X"
- 但这样审查者不知道：
  - 这个 campaign 是做什么的
  - 包含哪些工作流
  - 下一步要做什么

**设计智慧**：
> 自动化 PR 需要**比手动 PR 更详细**，因为它缺少人类的隐性知识。

---

### 观察 3：为什么有 "What You DON'T Do" 章节？

**推断原因**：

1. **防止职责蔓延**
   - Phase 2 Agent 可能"好心"想做更多
   - 显式禁止避免重复工作

2. **教育意图**
   - 告诉 Agent（和人类读者）：分工是故意设计的
   - 不是遗漏，而是架构决策

3. **调试线索**
   - 如果 Phase 2 失败，检查 Phase 1 是否完成
   - 边界清晰便于定位问题

**设计对比**：

| 设计方式 | 优势 | 劣势 |
|---------|------|------|
| 只说"做什么" | 简洁 | 容易职责蔓延 |
| 同时说"不做什么" | 边界清晰 | 文档稍长 |

**这个工作流选择了后者，说明**：
- 架构清晰度 > 文档简洁度
- 预防问题 > 事后修复

---

## 💡 批判性视角

### ✅ 做得好的地方

1. **职责边界清晰**
   - "What You DON'T Do" 防止蔓延
   - Phase 1/2 分工明确

2. **失败处理周全**
   - 文件不存在 → 报告停止
   - 编译错误 → 自动修复或报告
   - 所有失败都回到 Issue（可观测）

3. **状态传递显式**
   - 不依赖隐式状态
   - 所有输入都在"Context"章节声明

4. **用户引导到位**
   - PR 模板包含下一步
   - 成功报告包含性能指标

### 🤔 可以改进的地方

#### 1. **状态传递的脆弱性**

**观察**：
```markdown
You will be invoked with context containing:
- **Campaign ID**: `<campaign-id>`
```

**问题**：
- 如果调用者忘记传 `<campaign-id>` 怎么办？
- 文档没有说明如何验证这些参数

**改进建议**：
添加参数验证步骤：
```markdown
### Step 0: Validate Input Parameters

Check that all required context is provided:

```bash
if [ -z "$CAMPAIGN_ID" ]; then
  echo "Error: CAMPAIGN_ID not provided"
  exit 1
fi
```
```

---

#### 2. **编译失败的重试次数**

**观察**：
```markdown
- Re-compile until successful
```

**问题**：
- "直到成功"意味着无限重试吗？
- 如果是语法错误，可能永远无法自动修复

**改进建议**：
添加重试上限：
```markdown
- Re-compile (max 3 attempts)
- If still failing after 3 attempts, report to issue
```

---

#### 3. **缺少回滚机制**

**观察**：
- 如果 PR 创建失败，已经 commit 的文件怎么办？
- 是否需要回滚分支？

**改进建议**：
添加失败清理步骤：
```markdown
### Error Handling

If any step fails:
1. Post error details to issue
2. Delete the branch (optional: `git branch -D campaign/<campaign-id>`)
3. Leave campaign file for manual inspection
```

---

#### 4. **性能指标的依据**

**观察**：
```markdown
**Total time:** ~2-3 minutes (60% faster than old flow!)
```

**问题**：
- "60% faster" 的数据从哪里来？
- 是否应该动态计算实际时间？

**改进建议**：
```markdown
**Total time:** $(date) (measured: 2m 34s)
**Old flow time:** ~6-7 minutes
**Improvement:** ~60% faster
```

---

### ⚖️ 架构权衡分析

| 维度 | 双阶段架构 | 单阶段架构 |
|------|-----------|-----------|
| **复杂度** | ❌ 高（需要状态传递） | ✅ 低 |
| **环境限制** | ✅ 绕过（分阶段解决） | ❌ 受限 |
| **失败隔离** | ✅ 好（阶段独立） | ❌ 差 |
| **端到端时间** | ❌ 可能更长（两次运行） | ✅ 一次完成 |
| **调试难度** | ❌ 高（跨阶段问题） | ✅ 低 |

**结论**：
- 双阶段架构是**环境约束下的最优解**
- 不是为了炫技，而是**实用主义**的选择
- 如果未来 Actions 支持 CLI，可能会合并回单阶段

---

## 📦 可复用片段

### 片段 1：双阶段协作框架

**适用场景**：任务需要不同环境/工具的场景

```markdown
# Phase 2: [任务名称]

## Context

**Phase 1 (已完成):**
- ✅ [Phase 1 的职责 1]
- ✅ [Phase 1 的职责 2]

**Phase 2 (你的任务):**
- ⏳ [Phase 2 的职责 1]
- ⏳ [Phase 2 的职责 2]

## Why This Separation?

- [环境限制原因]
- [职责分离原因]

## Your Task

You will be invoked with context containing:
- **[参数 1]**: `<value>`
- **[参数 2]**: `<value>`

### Step 1: Verify Prerequisites
### Step 2: Execute Main Task
### Step 3: Report Results

## What You DON'T Do

**You do NOT:**
- ❌ [Phase 1 的职责]

**You ONLY:**
- ✅ [Phase 2 的职责]
```

**来源**: agentic-campaign-designer.agent.md  
**星级**: ⭐⭐⭐⭐⭐⭐⭐⭐（核心模式）

---

### 片段 2：显式状态契约

**适用场景**：需要明确输入参数的工作流

```markdown
## Input Contract

You will be invoked with context containing:
- **[参数名]**: `<placeholder>` - [参数用途]
- **[参数名]**: `<placeholder>` - [参数用途]

### Example Invocation

```
PARAM_1="value1" PARAM_2="value2" ./script.sh
```
```

**来源**: agentic-campaign-designer.agent.md  
**星级**: ⭐⭐⭐⭐⭐⭐

---

### 片段 3：渐进式错误恢复

**适用场景**：需要自动化修复但有兜底的场景

```markdown
**If [操作] fails:**
1. Review error messages
2. [自动修复步骤 1]
3. [自动修复步骤 2]
4. Re-try (max N attempts)
5. If still failing, report to [反馈渠道] and stop
```

**来源**: agentic-campaign-designer.agent.md  
**星级**: ⭐⭐⭐⭐⭐⭐

---

### 片段 4："What You DON'T Do" 边界声明

**适用场景**：防止职责蔓延的协作场景

```markdown
## What You DON'T Do

**You do NOT:**
- ❌ [其他阶段/组件的职责 1]
- ❌ [其他阶段/组件的职责 2]
- ❌ [其他阶段/组件的职责 3]

**You ONLY:**
- ✅ [本组件的核心职责 1]
- ✅ [本组件的核心职责 2]
- ✅ [本组件的核心职责 3]
```

**来源**: agentic-campaign-designer.agent.md  
**星级**: ⭐⭐⭐⭐⭐⭐⭐（架构清晰必备）

---

### 片段 5：详细的自动化 PR 模板

**适用场景**：自动化生成的 PR 需要完整上下文

```markdown
## [PR 标题]

Fixes #<issue-number>

### Purpose
[简短描述]

### Details
- **[关键参数 1]:** `<value>`
- **[关键参数 2]:** `<value>`

### Files Created
- `path/to/file1` (用途)
- `path/to/file2` (用途)

### Next Steps
1. [引导用户行为 1]
2. [引导用户行为 2]

### Links
- Original issue: #<issue-number>
- [相关文档链接]

---

**Generated by:** [生成方式说明]
**Time saved:** [性能指标]
```

**来源**: agentic-campaign-designer.agent.md (Step 4)  
**星级**: ⭐⭐⭐⭐⭐

---

## 📈 Skill 更新建议

### 1. 更新 `workflowAnalyzer/SKILL.md`

**添加新模式**：

```markdown
#### Two-Phase Collaboration Pattern ⭐⭐⭐⭐⭐⭐⭐⭐

**定义**: 将复杂任务拆分为两个独立阶段，每个阶段由不同的 Agent/环境执行，通过文件系统和 GitHub API 传递状态。

**识别特征**:
- Phase 1 在一个环境运行，Phase 2 在另一个环境运行
- 明确的阶段分界点（通常是文件写入）
- 显式的状态传递契约

**典型案例**: agentic-campaign-designer

**来源**: agentic-campaign-designer 分析 (运行 #22)
```

---

### 2. 更新 `workflowAuthoring/patterns/` 目录

**创建新文件**：`patterns/two-phase-collaboration.md`

```markdown
# Two-Phase Collaboration Pattern

## 模式概述

将复杂任务拆分为两个独立阶段，每个阶段由不同的 Agent/环境执行。

## 适用场景

- 不同阶段需要不同的工具/环境
- 长任务 + 短任务的组合（避免超时）
- 设计阶段 vs 执行阶段分离

## 设计要点

### 1. 明确阶段边界

**Phase 1 职责**:
- [设计相关任务]

**Phase 2 职责**:
- [执行相关任务]

### 2. 状态传递机制

| 传递方式 | 适用场景 |
|---------|---------|
| 文件系统 | 结构化数据 |
| Issue/PR | 元数据 |
| 环境变量 | 简单参数 |

### 3. 失败处理

- Phase 1 失败 → 不影响 Phase 2
- Phase 2 失败 → Phase 1 产物仍可用

## 代码示例

[引用 agentic-campaign-designer 的核心片段]

## 已知案例

- campaign-generator + agentic-campaign-designer

## 来源

agentic-campaign-designer 分析 (2026-01-09, 运行 #22)
```

---

### 3. 更新 `RESEARCH-AGENDA.md`

**P1 主题进展更新**：

```markdown
### P1 - 重要主题

| 主题 | 描述 | 期望产出 | **进展** |
|------|------|----------|----------|
| Agent 协作模式 | 多 Agent 间的协作、通信、任务分配机制 | 设计模式库、最佳实践 | **✅ 新增：Two-Phase Collaboration Pattern (2026-01-09)** |
```

---

## 🔮 后续研究方向

### 1. 深入研究 campaign-generator.md (Phase 1)

**动机**：
- 我们分析了 Phase 2，但 Phase 1 的设计同样重要
- Phase 1 如何发现工作流？（catalog 机制）
- Phase 1 如何生成 `.campaign.md`？（模板设计）

**预期收获**：
- 完整理解双阶段协作的全貌
- 学习"设计 Agent"的最佳实践

---

### 2. 研究工作流 catalog 机制

**动机**：
- campaign-generator 使用 "catalog (local + agentics collection)"
- 这是如何实现的？
- 是否可以应用到我们的 Skill 发现？

**观察线索**：
- commit #9450 添加了 `.github/workflow-catalog.yml`
- 可能是一个工作流索引机制

---

### 3. 对比旧版单阶段流程

**动机**：
- 了解为什么重构为双阶段
- 学习架构演进的思考过程

**研究方法**：
- 查看 Git 历史，找到重构前的版本
- 对比设计差异
- 总结重构决策过程

---

### 4. 探索 Copilot Coding Agent 的能力边界

**动机**：
- Phase 2 依赖 Copilot Session 的 CLI 能力
- 还有哪些能力是 Actions 没有的？
- 如何设计才能充分利用这些能力？

---

## 📝 关键发现总结

### 🎯 核心洞见

1. **双阶段协作不是"拆分任务"，而是"环境解耦"**
   - 根据环境能力划分阶段
   - 而非简单地分成"前半段"和"后半段"

2. **显式边界 > 隐式约定**
   - "What You DON'T Do" 防止职责蔓延
   - 状态契约让接口清晰

3. **自动化 PR 需要比手动 PR 更详细**
   - 因为缺少人类的隐性知识
   - 必须显式说明下一步行动

4. **失败处理分层：自动修复 → 重试 → 报告**
   - 最大化自动化率
   - 同时保持稳定性

### 📊 量化指标

| 指标 | 值 | 说明 |
|------|-----|------|
| **新模式数量** | 6 个 | Two-Phase, Explicit Contract, Verify-Before-Execute... |
| **可复用片段** | 5 个 | 双阶段框架、状态契约、错误恢复... |
| **Skill 空白填补** | 高 | Agent 协作模式的重要补充 |
| **主题匹配度** | 80 分 | 高度相关 P1 主题 |

---

## 🤔 反思与元认知

### 分析过程中的发现

1. **"为什么分阶段"比"怎么分阶段"更重要**
   - 文档花了很大篇幅解释设计理由
   - 这对理解架构至关重要

2. **边界声明是架构的核心**
   - "What You DON'T Do" 看似简单，实则关键
   - 防止了无数潜在的混乱

3. **状态传递的显式化是可维护性的关键**
   - 隐式状态（如内存、缓存）难以调试
   - 显式状态（文件、API）可追溯

### 如果让我设计这个工作流

**我可能会犯的错误**：
- ❌ 直接给出步骤，不解释"为什么分阶段"
- ❌ 假设 Agent 会"自己知道"不该做什么
- ❌ 使用简单的 PR 模板（缺少上下文）

**这个工作流教会了我**：
- ✅ 架构理由 > 实现细节
- ✅ 显式边界 > 隐式约定
- ✅ 预期失败 > 假设成功

---

*分析完成时间: 2026-01-09*  
*分析者: workflow-case-study Agent (运行 #22)*  
*置信度: ⭐⭐⭐⭐⭐（非常高）*
