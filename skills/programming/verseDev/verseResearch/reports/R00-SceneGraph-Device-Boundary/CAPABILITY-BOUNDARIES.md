# SceneGraph 能力边界文档

> **版本**: 2.0 | **更新日期**: 2026-01-05
>
> **重要更新**: 采用新心智模型 "**SG能力边界 = Component化边界**"
>
> **目标**: 明确 SceneGraph 的能力边界，以 Component化判定标准为核心

---

## 🎯 核心定义（v2.0 新模型）

### SG 能力边界 = Component化边界

**SceneGraph 适用范围**: 所有**可以 Component化**的功能

**判定标准**:
- ✅ **可抽象**: 能抽象为通用组件逻辑
- ✅ **可实例化**: 可运行时动态创建多个实例
- ✅ **数据驱动**: 行为由参数控制，非硬编码
- ✅ **可组合**: 可与其他组件组合
- ✅ **无编辑器依赖**: 不需要编辑器预置资源

**Device 适用范围**: **不能 Component化**的功能

**典型原因**:
- ❌ **编辑器预置**: 需要编辑器预先配置资源
- ❌ **实例化限制**: 不可动态创建，固定数量
- ❌ **引擎特殊支持**: 依赖引擎底层系统
- ❌ **资源绑定**: 绑定特定美术/音频资源

> 详见 [心智模型迁移指南](./MENTAL-MODEL-MIGRATION.md)

---

---

## Component化判定速查

### ✅ 可以 Component化（优先使用 SceneGraph）

| 判定维度 | 标准 | SceneGraph 能力 |
|---------|------|----------------|
| **逻辑抽象** | 可抽象为通用组件逻辑 | 状态机、计时器、对象池、AI行为树 |
| **实例化** | 可运行时动态创建多个实例 | `entity{}.AddComponents(array{...})` |
| **数据驱动** | 行为由数据参数控制 | 组件属性、配置结构体 |
| **可组合** | 可与其他组件组合 | 移动+碰撞+生命值组件组合 |
| **无编辑器依赖** | 不需要编辑器预置资源 | 纯代码逻辑、纯数据结构 |

**典型可 Component化场景**:
- ✅ 状态机（StateComponent）
- ✅ 生成器（SpawnerComponent）
- ✅ 属性系统（AttributeComponent）
- ✅ 计时器（TimerComponent）
- ✅ 碰撞检测（CollisionComponent）
- ✅ 事件总线（EventBusComponent）
- ✅ AI行为树（BehaviorTreeComponent）
- ✅ 对象池（ObjectPoolComponent）

### ❌ 不能 Component化（必须使用 Device）

| 判定维度 | 标准 | 典型 Device |
|---------|------|------------|
| **编辑器预置** | 需要编辑器预先配置资源/参数 | UI布局、音效资源 |
| **实例化限制** | 只能放置固定数量，不可动态创建 | 游戏规则、回合管理 |
| **引擎特殊支持** | 依赖引擎底层系统 | 玩家输入、物理引擎 |
| **全局单例** | 必须全局唯一 | ScoreManager、EndGame |
| **资源绑定** | 绑定特定美术/音频资源 | AudioPlayer、MeshRenderer |

**典型不可 Component化场景**:
- ❌ 玩家输入（input_trigger_device）—— 引擎特殊支持
- ❌ UI显示（hud_message_device）—— 编辑器预置 + 引擎渲染
- ❌ 音效播放（audio_player_device）—— 资源绑定
- ❌ 游戏规则（end_game_device）—— 全局单例
- ❌ 得分统计（score_manager_device）—— 全局单例
- ❌ 玩家传送（teleporter_device）—— 编辑器预置位置

### ⚙️ 混合架构（SG逻辑 + Device能力）

**模式**: Component 持有状态和逻辑，Device 提供输入/输出/资源能力

| 功能 | Component部分 | Device部分 |
|------|--------------|-----------|
| **钓鱼系统** | 钓鱼状态机、进度计算 | 输入检测、UI显示、音效 |
| **波次生成** | 波次管理、敌人生成 | 音效播放、UI提示 |
| **技能系统** | 技能冷却、效果计算 | 输入检测、UI显示 |

---

## SceneGraph 核心能力矩阵

### 能做的事（绿灯区）

| 类别 | 具体能力 | API 支持 | Component化判定 |
|------|----------|----------|----------------|
| **实体管理** | 创建、销毁、层级化组织实体 | `entity`, `AddEntities()`, `RemoveFromParent()` | ✅ 可实例化、可组合 |
| **组件系统** | 自定义组件、挂载/卸载、生命周期管理 | `component`, `AddComponents()`, `GetComponent<T>()` | ✅ 可抽象、数据驱动 |
| **事件通信** | SendUp/SendDown/SendDirect 三种传播 | `SendUp()`, `SendDown()`, `SendDirect()`, `OnReceive()` | ✅ 可组合、解耦通信 |
| **异步流程** | spawn 协程、Sleep 延迟、race 竞态 | `spawn{}`, `Sleep()`, `race{}`, `sync{}` | ✅ 纯逻辑、可抽象 |
| **空间查询** | 碰撞检测、Overlap 查询、Sweep 扫描 | `FindOverlapHits()`, `FindSweepHits()` | ✅ 无编辑器依赖 |
| **变换操作** | 位置、旋转、缩放控制 | `transform_component`, `GetPosition()`, `SetPosition()` | ✅ 数据驱动 |
| **层级遍历** | 父子关系查询、树形结构导航 | `GetParent()`, `GetEntities()`, `GetComponents()` | ✅ 可组合 |
| **数据容器** | array、map、option、集合操作 | Verse 标准库 | ✅ 纯逻辑 |
| **生命周期** | OnAddedToScene/OnBeginSimulation/OnSimulate/OnDestroy | 组件生命周期方法 | ✅ 可抽象 |
| **Prefab** | 预制件实例化、复用实体组合 | UEFN 编辑器 + SceneGraph | ✅ 可实例化 |

### 不能做的事（红灯区）

| 类别 | 限制说明 | 不能 Component化原因 | 替代方案 |
|------|----------|---------------------|----------|
| **发布限制** | ⚠️ **使用 SceneGraph 的项目无法发布** | Beta 功能限制 | 等待 Epic 解除限制或发布前禁用 SG |
| **玩家输入** | 无法直接获取玩家输入 | 引擎特殊支持 | `input_trigger_device` |
| **UI 显示** | 无法创建 UI 界面、HUD、菜单 | 编辑器预置 + 引擎渲染 | `hud_message_device`, `canvas_device` |
| **游戏规则** | 无法设置回合、得分、胜利条件 | 全局单例、引擎特殊支持 | `end_game_device`, `score_manager_device` |
| **资源加载** | 无法动态加载外部资源 | 资源绑定、编辑器预置 | 编辑器预配置或 Device |
| **网络同步** | 无原生网络复制支持 | 引擎底层系统 | 依赖 Fortnite 底层网络系统 |
| **持久化** | 无法保存数据到磁盘或云端 | 引擎特殊支持 | `accolades_device` 或外部服务 |
| **物理模拟** | 无法创建物理约束、关节 | 引擎特殊支持 | `physics_device` |
| **动画控制** | 无法播放骨骼动画、Montage | 资源绑定 | `animation_controller_device` |
| **音频播放** | 无法播放音效、背景音乐 | 资源绑定 | `audio_player_device` |

### 混合使用的事（黄灯区）

| 类别 | Component部分 | Device部分 | 协作方式 |
|------|--------------|-----------|----------|
| **玩家追踪** | 玩家列表管理、状态追踪 | 获取 agent 引用 | Device 事件传入 agent |
| **伤害系统** | 生命值计算、状态管理 | agent.damage() 调用 | Component 封装逻辑 |
| **计时器** | 时间管理、回调触发 | - | 纯 Component（spawn + Sleep） |
| **状态机** | 状态存储、转换逻辑 | - | 纯 Component（组件 + 事件） |
| **对象池** | 实例管理、复用逻辑 | - | 纯 Component（数组管理） |
| **事件总线** | 事件路由、订阅管理 | - | 纯 Component（SendDown广播） |

---

## 📊 决策流程

### Component化判定流程

```
收到功能需求
    │
    ▼
能否抽象为通用组件逻辑？
    │
    ├─ 能 → 继续检查
    │       │
    │       ▼
    │   能否运行时动态创建多个实例？
    │       │
    │       ├─ 能 → ✅ SceneGraph (entity + component)
    │       │       │
    │       │       └─ 需要输入/UI/音频？ → 混合 Device
    │       │
    │       └─ 不能 → 检查原因
    │               │
    │               ├─ 业务需要单例 → ✅ SceneGraph (单例 component)
    │               │
    │               └─ 技术限制 → ❌ Device
    │
    └─ 不能 → 检查原因
            │
            ├─ 编辑器预置资源 → ❌ Device
            ├─ 实例化技术限制 → ❌ Device
            ├─ 引擎底层系统依赖 → ❌ Device
            └─ 资源绑定 → ❌ Device
```

### 快速决策表

| 问题 | 是 | 否 |
|------|----|----|
| 能抽象为通用逻辑？ | 继续 → | ❌ Device |
| 可动态创建多实例？ | ✅ SG | 检查原因 → |
| 需要编辑器预置？ | ❌ Device | ✅ SG |
| 依赖引擎底层系统？ | ❌ Device | ✅ SG |
| 绑定美术/音频资源？ | ❌ Device | ✅ SG |

---

## 典型案例分析（基于新模型）

### 案例1: 钓鱼系统

**Component化判定**:
| 功能模块 | 可抽象？ | 可实例化？ | 判定结果 |
|---------|---------|-----------|---------|
| 钓鱼状态机 | ✅ 通用状态机模式 | ✅ 可多钓鱼点 | ✅ Component |
| 进度计算 | ✅ 纯数学逻辑 | ✅ 无限制 | ✅ Component (Helper) |
| 玩家输入检测 | ❌ 引擎输入系统 | - | ❌ Device |
| UI进度条 | ❌ 编辑器UI布局 | - | ❌ Device |
| 音效播放 | ❌ 资源绑定 | - | ❌ Device |

**架构方案**:
```verse
# Component 持有状态和逻辑
fishing_component := class(component):
    var State:fishing_state = fishing_state.Idle
    var Progress:float = 0.0
    
    # Device 引用（编辑器配置）
    @editable InputDevice:input_trigger_device
    @editable AudioDevice:audio_player_device
    @editable UIDevice:canvas_device
    
    OnBeginSimulation<override>()<suspends>:void =
        Sleep(0.0)
        InputDevice.InteractedWithEvent.Subscribe(OnPlayerInteract)
    
    # 逻辑在 Component
    OnPlayerInteract(Agent:?agent):void =
        if (State = fishing_state.Idle):
            StartFishing()
    
    StartFishing()<suspends>:void =
        set State = fishing_state.Casting
        AudioDevice.Play()  # Device 提供能力
        spawn { UpdateProgress() }
```

---

### 案例2: 敌人波次生成

**Component化判定**:
| 功能模块 | 可抽象？ | 可实例化？ | 判定结果 |
|---------|---------|-----------|---------|
| 波次管理 | ✅ 通用管理模式 | ✅ 可多波次点 | ✅ Component |
| 敌人生成 | ✅ SceneGraph实体创建 | ✅ 动态创建 | ✅ Component |
| 路径规划 | ✅ AI逻辑 | ✅ 可组件化 | ✅ Component |
| 敌人死亡音效 | ❌ 资源绑定 | - | ❌ Device |
| 波次完成UI | ❌ UI显示 | - | ❌ Device |

**架构方案**:
```verse
# 波次管理 Component
wave_manager_component := class(component):
    var CurrentWave:int = 1
    var EnemiesRemaining:int = 0
    
    @editable EnemyPrefab:entity
    @editable AudioDevice:audio_player_device
    
    SpawnWave():void =
        EnemyCount := CalculateEnemyCount(CurrentWave)
        for (I := 0..EnemyCount):
            SpawnEnemy(I)
    
    # 动态创建实体（SceneGraph能力）
    SpawnEnemy(Index:int):void =
        NewEnemy := enemy_entity{}
        NewEnemy.AddComponents(array{
            enemy_ai_component{},
            health_component{}
        })
        if (Owner := GetOwner()):
            Owner.AddEntities(array{NewEnemy})
```

---

### 案例3: UI菜单系统

**Component化判定**:
| 功能模块 | 可抽象？ | 可实例化？ | 判定结果 |
|---------|---------|-----------|---------|
| UI布局 | ❌ 编辑器预置 | - | ❌ Device |
| 菜单状态 | ✅ 状态机模式 | ✅ 可选 | ⚠️ Component (可选) |
| 按钮逻辑 | ❌ Device事件系统 | - | ❌ Device主导 |

**架构方案**:
```verse
# 可选：复杂菜单状态用 Component 管理
menu_state_component := class(component):
    var CurrentMenu:menu_type = menu_type.Main
    var SelectedOption:int = 0
    
    @editable CanvasDevice:canvas_device
    
    SwitchMenu(NewMenu:menu_type):void =
        set CurrentMenu = NewMenu
        UpdateUI()  # 通过 Device 渲染
    
    UpdateUI():void =
        # Device 提供显示能力
        CanvasDevice.SetText(GetMenuText())
```

**简化方案**（小型菜单）:
```verse
# 纯 Device，无需 Component
# 所有逻辑在 Device 事件处理中
```

---

## 关键能力详解（基于 Component化视角）

### 1. 实体与组件系统

**Component化能力**: ✅ **完全支持**

**为何可 Component化**:
- ✅ 可抽象为通用组件模式
- ✅ 可运行时动态创建多个实例
- ✅ 数据驱动，参数可配置
- ✅ 可组合（多组件协作）

**核心能力**:
- **实体创建**: 可运行时创建自定义 entity 类实例
- **层级管理**: 支持任意深度的父子关系（推荐 3-4 层）
- **组件挂载**: 动态添加/移除组件
- **生命周期**: 完整的生命周期钩子（OnAddedToScene → OnBeginSimulation → OnSimulate → OnDestroy）

**示例**:

```verse
# 创建自定义实体类（Component化模式）
game_manager_entity := class(entity):
    var GameState<private>:game_state = game_state.Waiting

    Initialize():void =
        AddComponents(array{
            round_timer_component{},
            score_tracker_component{},
            state_machine_component{}
        })

# 运行时动态创建（SceneGraph 能力）
Manager := game_manager_entity{}
Manager.Initialize()
SimulationRoot.AddEntities(array{Manager})
```

### 2. 事件系统

**Component化能力**: ✅ **完全支持**

**为何可 Component化**:
- ✅ 可抽象为通用事件模式
- ✅ 解耦通信，可组合
- ✅ 数据驱动（自定义事件数据）

**核心能力**:
- **三种传播**: SendUp（子→父）、SendDown（父→子）、SendDirect（点对点）
- **自定义事件**: 继承 `scene_event` 定义任意数据结构
- **事件消耗**: 同实体内所有组件都收到事件，跨实体可被消耗

**示例**:

```verse
# 定义事件（Component化数据结构）
player_scored_event := class<concrete>(scene_event):
    var Player:agent
    var Points:int

# 发送事件（Component 发起）
scoring_zone_component := class(component):
    OnPlayerEnter(Player:agent):void =
        if (Owner := GetOwner()):
            Owner.SendUp(player_scored_event{Player := Player, Points := 100})

# 接收事件（Component 响应）
score_tracker_component := class(component):
    OnReceive<override>(Event:scene_event):logic =
        if (ScoreEvent := Event?player_scored_event):
            UpdateScore(ScoreEvent.Player, ScoreEvent.Points)
            return true
        return false
```

### 3. 异步机制

**Component化能力**: ✅ **完全支持**

**为何可 Component化**:
- ✅ 纯语言特性，可抽象
- ✅ 无编辑器依赖
- ✅ 可组合（协程 + 组件生命周期）

**核心能力**:
- **spawn**: 创建并发协程，不阻塞主流程
- **Sleep**: 延迟执行（秒为单位）
- **race**: 竞态执行，首个完成即返回
- **sync**: 等待所有协程完成

**示例**:

```verse
# 定时器实现
timer_component := class(component):
    var IsRunning<private>:logic = false
    var ElapsedTime<private>:float = 0.0

    OnBeginSimulation<override>()<suspends>:void =
        Sleep(0.0)  # 必须！官方推荐
        StartTimer()

    StartTimer()<suspends>:void =
        IsRunning = true
        spawn:
            loop:
                if not IsRunning:
                    break
                Sleep(0.1)
                ElapsedTime += 0.1
                CheckTimeout()
```text

### 4. 数据结构

### ✅ 部分支持

**原生支持**:

- `array<T>`: 动态数组
- `map<K, V>`: 键值对字典
- `option<T>`: 可选值
- `generator<T>`: 生成器（惰性序列）
- 基础类型: `int`, `float`, `string`, `logic`, `vector3`, `rotation`

**不支持**:

- Set（集合）: 需自行用 `map<T, logic>` 模拟
- 多线程安全容器: Verse 无多线程概念

**玩家追踪示例**:

```verse
# 追踪所有玩家
player_tracker_component := class(component):
    var Players<private>:[]agent = array{}
    var PlayerData<private>:map(agent, player_data) = map{}

    AddPlayer(Player:agent):void =
        set Players += Player
        if:
            set PlayerData[Player] = player_data{Health := 100, Score := 0}

    RemovePlayer(Player:agent):void =
        set Players = Players.RemoveElement(Player)
        if:
            set PlayerData = PlayerData.RemoveKey(Player)
```text

---

## 典型 UseCase（基于 Component化判定）

### ✅ 可 Component化（SceneGraph 独立实现）

| 场景 | 实现方式 | 核心组件 |
|------|----------|----------|
| **对象生成系统** | 定时器 + 实体池 | spawn + array 管理 |
| **碰撞检测** | FindOverlapHits + 事件通知 | 空间查询 API |
| **状态机** | 组件存储状态 + 事件驱动转换 | OnReceive + 状态枚举 |
| **计时器** | spawn + Sleep 循环 | 异步流程 |
| **层级管理** | 父子实体嵌套 | AddEntities + GetParent |
| **事件总线** | 根实体广播 | SendDown 全局通知 |
| **数据同步** | 事件 + map 存储 | 自定义数据组件 |
| **AI 行为树** | 组件 + 状态机 + 定时器 | 组合上述机制 |

### ❌ 不可 Component化（必须使用 Device）

| 场景 | 原因 | 需要的 Device |
|------|------|---------------|
| **玩家输入** | 无输入 API | `input_trigger_device` |
| **UI 显示** | 无 UI API | `hud_message_device`, `billboard_device` |
| **音效播放** | 无音频 API | `audio_player_device` |
| **回合规则** | 无游戏规则 API | `end_game_device`, `round_settings_device` |
| **得分统计** | 无得分 API | `score_manager_device` |
| **物品生成** | 无物品 API | `item_spawner_device` |
| **玩家传送** | 无传送 API | `teleporter_device` |
| **伤害系统** | 需要 agent 引用 | `damage_volume_device` 获取 agent |

---

## 已知限制与警告

### 🔴 核心限制

1. **发布限制（最重要）**
   - SceneGraph 是 Beta 功能
   - **使用 SceneGraph 的项目无法发布到 Fortnite**
   - Epic 正在验证稳定性，未来可能解除

2. **无原生玩家管理**
   - 无法直接获取 `GetPlayers()` 等 API
   - 需要通过 Device 事件（如 `player_spawned_event`）获取 agent 引用
   - 获取后可在 SceneGraph 内自行追踪

3. **无 UI 支持**
   - 无法创建按钮、面板、HUD
   - 所有 UI 必须通过 Device 实现

4. **资源限制**
   - 无法动态加载 Mesh、材质、音频
   - 所有资源必须在编辑器中预配置

### ⚠️ 性能警告

1. **OnSimulate 开销**
   - 每帧调用，避免复杂计算
   - 推荐用 spawn + Sleep 实现定时逻辑

2. **深层嵌套**
   - 过深的实体层级影响性能
   - 推荐扁平化设计或限制在 3-4 层

3. **事件风暴**
   - SendDown 会递归所有子实体
   - 避免频繁广播大量事件

### 💡 最佳实践

1. **Sleep(0.0) 必须**
   - OnBeginSimulation 第一行必须 `Sleep(0.0)`
   - 延迟一帧确保引擎初始化完成

2. **组件单一职责**
   - 每个组件只做一件事
   - 通过事件解耦通信

3. **混合架构**
   - 复杂系统用自定义 Entity 类封装
   - 简单对象用纯组件方式

---

## 边界验证来源

| 能力 | 验证方式 | 文档链接 |
|------|----------|----------|
| 实体/组件 API | 官方 API 文档 | [entity](https://dev.epicgames.com/documentation/en-us/fortnite/verse-api/versedotorg/scenegraph/entity), [component](https://dev.epicgames.com/documentation/en-us/fortnite/verse-api/versedotorg/scenegraph/component) |
| 事件系统 | 官方教程 | [Scene Events](https://dev.epicgames.com/documentation/en-us/fortnite/scene-events-in-unreal-editor-for-fortnite) |
| 异步机制 | Verse 语言规范 | Verse 语言特性 |
| 发布限制 | 官方文档警告 | [SceneGraph 概述](https://dev.epicgames.com/documentation/en-us/fortnite/scene-graph-in-unreal-editor-for-fortnite) |
| Device 依赖 | 设备系统调研 | [UEFN 设备系统调研](../../shared/references/uefn-device-system-research.md) |

---

## 快速决策流程图

```text
需求：实现某功能
    │
    ▼
是否涉及玩家输入/UI/音频/物理？
    │
    ├─ 是 → 必须使用 Device
    │
    └─ 否 → 检查下一步
         │
         ▼
    是否需要游戏规则（回合、得分）？
         │
         ├─ 是 → 必须使用 Device
         │
         └─ 否 → 检查下一步
              │
              ▼
         是否需要动态加载资源？
              │
              ├─ 是 → 必须使用 Device
              │
              └─ 否 → ✅ 可用 SceneGraph 独立实现
```text

---

## 待验证的未知区

| 类别 | 不确定的能力 | 验证方法 |
|------|--------------|----------|
| 网络同步 | 多人游戏下实体同步行为 | 实际测试 |
| 性能上限 | 大量实体/组件的性能表现 | 压力测试 |
| 跨 Prefab 通信 | Prefab 实例间事件传播 | 实验验证 |
| Device 交互 | SG 与 Device 的混合使用模式 | 案例研究 |

---

## 📝 v2.0 总结：Component化判定核心

### 核心原则

**SG 能力边界 = Component化边界**

### 判定标准（5项检查）

- [ ] **可抽象**: 能抽象为通用组件逻辑？
- [ ] **可实例化**: 可运行时动态创建多个实例？
- [ ] **数据驱动**: 行为由参数控制，非硬编码？
- [ ] **可组合**: 可与其他组件组合形成复杂行为？
- [ ] **无编辑器依赖**: 不需要编辑器预置资源？

**满足 3 项以上** → ✅ 可 Component化（优先使用 SceneGraph）

### 架构决策

| 判定结果 | 架构方案 | 示例 |
|---------|---------|------|
| **全部可 Component化** | 纯 SceneGraph | 状态机、生成器、AI行为树 |
| **部分可 Component化** | 混合架构（SG逻辑 + Device能力） | 钓鱼系统、波次生成 |
| **不可 Component化** | 纯 Device | UI菜单、音效管理 |

### 混合架构模式

**Component 职责**:
- ✅ 持有所有状态和逻辑
- ✅ 管理游戏业务规则
- ✅ 派发事件通知

**Device 职责**:
- ✅ 提供输入能力（玩家交互）
- ✅ 提供输出能力（UI、音效）
- ✅ 提供资源能力（美术、音频资源）

**协作方式**:
- Device 事件触发 Component 逻辑
- Component 通过 Device 引用调用能力
- 编辑器中配置 Device，代码中 `@editable` 引用

---

## 📖 相关文档

| 文档 | 说明 |
|------|------|
| [MENTAL-MODEL-MIGRATION.md](./MENTAL-MODEL-MIGRATION.md) | 心智模型迁移完整指南 |
| [README.md](./README.md) | R00 调研报告索引 |
| [01-entity-component.md](./01-entity-component.md) | 实体与组件系统详解 |
| [02-event-system.md](./02-event-system.md) | 事件系统详解 |
| [05-use-cases.md](./05-use-cases.md) | 典型用例分析 |

---

**更新日志**:

- **2026-01-05 (v2.0)**: 采用新心智模型 "SG能力边界 = Component化边界"
  - 新增：Component化判定标准（5项检查）
  - 新增：决策流程图
  - 新增：典型案例分析（基于新模型）
  - 新增：混合架构模式详解
  - 更新：所有能力矩阵增加 Component化判定维度
  - 参考：[MENTAL-MODEL-MIGRATION.md](./MENTAL-MODEL-MIGRATION.md)
- **2026-01-05 (v1.0)**: 初始版本，基于官方文档和现有参考资料整理
