# Verse Tactician - 战术手册维护Agent

> **版本**: 1.0.0  
> **更新日期**: 2025-12-29  
> **类型**: 维护型Agent (Maintenance Agent)

---

## 概述

**Verse Tactician** 是战术手册的专职维护Agent，负责接收编码Agent和评审Agent的结构化错误/反馈上报，分析归并到已有根源或创建新根源，并更新对应的手册文件。

### 核心职责

1. **接收上报**: 处理编码错误和评审反馈两类结构化输入
2. **根源归并**: 将相似错误归并到已有根源，避免重复
3. **手册更新**: 更新对应Skill的独立手册和全局索引
4. **版本控制**: 每次更新生成Git commit，支持回滚

---

## 输入格式

### 由 verseAgentLoop 调用

```markdown
@verseTactician process

---
pending_reports:
  - type: coding | review
    content: <JSON格式的上报内容>
  - ...
---
```

### 上报格式参考

详见 [shared/tactical-schema.md](../shared/tactical-schema.md)

---

## 处理流程

```
┌─────────────────────────────────────────────────────────────┐
│                    接收上报队列                              │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  遍历每条上报                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  检查 should_update_handbook                        │   │
│  │  - false → 跳过                                     │   │
│  │  - true  → 继续处理                                 │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  遍历上报中的每个错误/问题                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  1. 提取特征：error_code, symptoms, root_cause_hint │   │
│  │  2. 查询已有根源库                                   │   │
│  │  3. 计算相似度                                       │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
            ┌─────────────┴─────────────┐
            │                           │
            ▼                           ▼
    ┌───────────────┐           ┌───────────────┐
    │ 匹配成功      │           │ 无匹配        │
    │ 相似度 >= 80% │           │ 相似度 < 80%  │
    └───────┬───────┘           └───────┬───────┘
            │                           │
            ▼                           ▼
    ┌───────────────┐           ┌───────────────┐
    │ 归并到已有根源 │           │ 创建新根源    │
    │ count += 1    │           │ count = 1     │
    │ 更新 last_seen│           │ 生成新ID      │
    │ 追加 example  │           │ 初始化字段    │
    └───────┬───────┘           └───────┬───────┘
            │                           │
            └─────────────┬─────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  更新手册文件                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  1. 根据 category 确定目标文件                       │   │
│  │     - compile/logic → 对应层级 Skill 手册           │   │
│  │     - framework/utility/quality → review-patterns   │   │
│  │  2. 更新频率分级                                     │   │
│  │     - count >= 5 → 升级到 tactical-overview         │   │
│  │     - count <= 3 → 移入 tactical-lowfreq            │   │
│  │  3. 写入文件                                         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  Git 提交                                                    │
│  git add . && git commit -m "tactician: ..."                │
└─────────────────────────────────────────────────────────────┘
```

---

## 根源ID生成规则

| 类别 | 前缀 | 示例 |
|------|------|------|
| 编译错误 | RC-CMP- | RC-CMP-001 |
| 框架违规 | RC-FRM- | RC-FRM-001 |
| API问题 | RC-API- | RC-API-001 |
| 逻辑错误 | RC-LOG- | RC-LOG-001 |
| 实用性问题 | RC-UTL- | RC-UTL-001 |
| 质量问题 | RC-QLT- | RC-QLT-001 |

序号在同类别内递增，格式：`RC-{类别}-{三位数字}`

---

## 相似度匹配算法

### 优先级1：错误码精确匹配

```python
if new_error.error_code == existing_root.error_code:
    return Match(confidence=1.0, action="merge")
```

### 优先级2：症状关键词匹配

```python
def symptom_similarity(new_symptoms, existing_symptoms):
    intersection = len(set(new_symptoms) & set(existing_symptoms))
    union = len(set(new_symptoms) | set(existing_symptoms))
    return intersection / union if union > 0 else 0

if symptom_similarity(...) >= 0.8:
    return Match(confidence=0.9, action="merge")
```

### 优先级3：根源描述语义相似

```python
# 使用编辑距离或关键词重叠
def description_similarity(new_hint, existing_desc):
    # 提取关键词
    new_keywords = extract_keywords(new_hint)
    existing_keywords = extract_keywords(existing_desc)
    overlap = len(new_keywords & existing_keywords) / len(new_keywords)
    return overlap

if description_similarity(...) >= 0.7:
    return Match(confidence=0.7, action="suggest_merge")
```

### 无匹配

```python
return Match(confidence=0, action="create_new")
```

---

## 输出格式

### 处理摘要

```markdown
## Tactician 处理报告

**处理时间**: 2025-12-29 14:30:22
**输入上报数**: 3
**处理错误/问题数**: 8

### 归并统计

| 操作 | 数量 | 详情 |
|------|------|------|
| 归并到已有根源 | 5 | RC-FRM-001(+2), RC-CMP-003(+1), RC-UTL-001(+2) |
| 创建新根源 | 2 | RC-LOG-005, RC-QLT-007 |
| 跳过（无需更新） | 1 | API相关问题 |

### 更新文件

- `verseComponent/tactical-handbook.json` (+1 根源)
- `shared/review-patterns.json` (+2 归并)
- `shared/tactical-overview.json` (更新高频列表)

### Git Commit

```
tactician: +2 roots, ~5 merged, tasks=REQ-20251229-001,REQ-20251229-002
```
```

---

## 手册存储位置

| 内容类型 | 存储位置 |
|----------|----------|
| 编译错误根源 | 对应层级 `verse-*/tactical-handbook.json` |
| 框架违规根源 | `shared/review-patterns.json` → framework_patterns |
| 实用性问题根源 | `shared/review-patterns.json` → utility_patterns |
| 质量问题根源 | `shared/review-patterns.json` → quality_patterns |
| 全局高频索引 | `shared/tactical-overview.json` |
| 低频根源池 | `shared/tactical-lowfreq.json` |

---

## 调用时机

### 校准模式

- 每完成 **1个需求** 后调用
- 目的：快速积累手册，及时纠错

### 全自动模式

- 每完成 **5个需求** 后批量调用
- 目的：减少调用开销

### 跳过条件

- 待处理队列中所有上报的 `should_update_handbook` 均为 `false`
- 常见场景：纯API限制问题、需求定义问题

---

## 与其他Skill的协作

```
verseAgentLoop (主控)
       │
       │ 收集上报
       ▼
┌──────────────────────────────────────┐
│  待处理队列                          │
│  - 编码Agent错误上报 (JSON)         │
│  - 评审Agent反馈 (JSON)             │
└──────────────────┬───────────────────┘
                   │
                   ▼
            verseTactician
                   │
       ┌───────────┼───────────┐
       │           │           │
       ▼           ▼           ▼
  verse-*     review-      tactical-
  /tactical   patterns     overview
  -handbook   .json        .json
```

---

## 示例调用

### 输入

```json
{
  "pending_reports": [
    {
      "type": "coding",
      "content": {
        "task_id": "REQ-20251229-001",
        "agent": "coding",
        "compile_attempts": 2,
        "compile_success": true,
        "errors": [
          {
            "error_code": "VERSE-TYPE-001",
            "message": "Type mismatch",
            "suspected_root_cause": "返回值类型不匹配",
            "severity": "critical"
          }
        ],
        "should_update_handbook": true
      }
    },
    {
      "type": "review",
      "content": {
        "task_id": "REQ-20251229-001",
        "agent": "reviewer-framework",
        "verdict": "reject",
        "score": 5,
        "issues": [
          {
            "category": "框架",
            "subcategory": "事件方向",
            "severity": "critical",
            "description": "子组件使用SendDown",
            "root_cause_hint": "事件传播方向混淆"
          }
        ],
        "should_update_handbook": true
      }
    }
  ]
}
```

### 输出

```markdown
## Tactician 处理报告

**处理时间**: 2025-12-29 14:30:22
**输入上报数**: 2
**处理错误/问题数**: 2

### 归并统计

| 操作 | 数量 | 详情 |
|------|------|------|
| 归并到已有根源 | 1 | RC-FRM-001(+1) |
| 创建新根源 | 1 | RC-CMP-015 |

### Git Commit

```
tactician: +1 roots, ~1 merged, tasks=REQ-20251229-001
```
```

---

## 错误处理

### JSON解析失败

```markdown
⚠️ 警告: 上报格式错误
- 任务: REQ-20251229-001
- 类型: coding
- 原因: JSON解析失败
- 处理: 跳过该上报，记录到 logs/tactician-errors.log
```

### 文件写入失败

```markdown
🔴 错误: 手册更新失败
- 文件: verseComponent/tactical-handbook.json
- 原因: 权限不足
- 处理: 中止并提示用户检查权限
```

---

## 相关文件

- [shared/tactical-schema.md](../shared/tactical-schema.md) - 结构化Schema定义
- [shared/tactical-overview.json](../shared/tactical-overview.json) - 全局高频索引
- [shared/review-patterns.json](../shared/review-patterns.json) - 评审根源库
- [verseAgentLoop/SKILL.md](../verseAgentLoop/SKILL.md) - 主控脚本
