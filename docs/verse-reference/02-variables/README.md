# Verse 变量与绑定调研报告

本目录包含关于 Verse 编程语言中变量、常量和绑定机制的深入调研报告。

## 📚 文档目录

### [01-declaration.md](./01-declaration.md) - 变量声明三模式
深入探讨 Verse 中的三种变量/常量声明方式：
- **常量（不可变）**：`Identifier : type = value`
- **类型推断常量**：`Identifier := value`（仅局部作用域）
- **可变变量**：`var Identifier : type = value`

**核心主题**：
- `:=` 与 `=` 的区别
- 何时使用哪种声明方式
- 可变性最佳实践

### [02-scope.md](./02-scope.md) - 作用域规则
解析 Verse 的作用域系统和变量可见性规则：
- **块作用域**：代码块内的局部标识符
- **函数作用域**：函数参数和函数体内标识符
- **模块作用域**：模块级标识符和访问修饰符

**核心主题**：
- Verse 禁止变量遮蔽的独特设计
- 限定符（Qualifier）访问同名标识符
- 作用域最小化原则

### [03-constants.md](./03-constants.md) - 常量与编译时求值
研究 Verse 的常量系统和 `<computes>` 效果标记：
- **纯函数标记**：`<computes>` 的语义承诺
- **编译时优化**：常量折叠和内联
- **确定性计算**：无副作用的函数式编程

**核心主题**：
- `<computes>` 的使用场景和限制
- 纯函数 vs 副作用函数
- 编译器优化机会

### [04-reference-value.md](./04-reference-value.md) - 引用与值语义
对比 Verse 中的值类型和引用类型：
- **值语义**：struct、基础类型、容器（复制）
- **引用语义**：class、interface（共享）
- **对象共享行为**：意外共享的陷阱

**核心主题**：
- struct vs class 选择指南
- 避免意外引用共享
- 不可变容器的优势

### [05-pattern-binding.md](./05-pattern-binding.md) - 模式匹配绑定
详解 Verse 的模式匹配和条件绑定机制：
- **`if (x := expr)` 语法**：失败上下文中的绑定
- **可失败表达式**：数组索引、映射查找、比较
- **多重绑定**：组合多个条件

**核心主题**：
- 失败即控制流的设计哲学
- 绑定 vs 断言
- 安全访问可选值

## 🎯 快速导航

### 按使用场景

| 场景 | 推荐文档 | 关键内容 |
|------|---------|---------|
| 声明变量/常量 | [01-declaration.md](./01-declaration.md) | 三种声明模式、`:=` vs `=` |
| 理解作用域 | [02-scope.md](./02-scope.md) | 三级作用域、禁止遮蔽 |
| 编写纯函数 | [03-constants.md](./03-constants.md) | `<computes>` 标记、优化 |
| 选择数据类型 | [04-reference-value.md](./04-reference-value.md) | struct vs class 决策树 |
| 安全访问数据 | [05-pattern-binding.md](./05-pattern-binding.md) | 绑定语法、失败处理 |

### 按开发阶段

**学习阶段**：
1. 先读 [01-declaration.md](./01-declaration.md) 理解基础声明
2. 再读 [02-scope.md](./02-scope.md) 掌握作用域规则
3. 然后 [05-pattern-binding.md](./05-pattern-binding.md) 学习安全访问

**优化阶段**：
- [03-constants.md](./03-constants.md) 学习性能优化
- [04-reference-value.md](./04-reference-value.md) 优化内存使用

## 📋 核心概念速查

### 声明语法速查

```verse
# 常量（显式类型）- 模块级和函数级
MaxHealth : int = 100

# 常量（类型推断）- 仅函数级
LocalValue := 50

# 变量（可变）
var CurrentHealth : int = 100
set CurrentHealth = 50
```

### 作用域速查

```verse
# 模块作用域
GlobalConstant : int = 100

MyFunction():void=
    # 函数作用域
    LocalConstant := 50
    
    if (condition):
        # 块作用域
        BlockConstant := 25
```

### 类型语义速查

```verse
# 值类型（复制）
position := struct{X := 0, Y := 0}
P2 := P1  # 复制

# 引用类型（共享）
player := class{var Score:int = 0}
P2 := P1  # 共享引用
```

### 绑定语法速查

```verse
# 单一绑定
if (Value := Array[0]):
    Print("{Value}")

# 多重绑定
if (First := Array[0], Second := Array[1]):
    Print("{First}, {Second}")

# 绑定 + 断言
if (Value := Array[0], Value > 10):
    Print("Large: {Value}")
```

## 🔍 常见问题索引

### Q1: 何时使用 `:=` 何时使用 `=`？
→ 见 [01-declaration.md](./01-declaration.md#:-与--的核心区别)

### Q2: Verse 为什么不允许变量遮蔽？
→ 见 [02-scope.md](./02-scope.md#变量遮蔽规则)

### Q3: 什么是 `<computes>` 函数？
→ 见 [03-constants.md](./03-constants.md#computes-效果标记)

### Q4: struct 和 class 有什么区别？
→ 见 [04-reference-value.md](./04-reference-value.md#值类型-vs-引用类型)

### Q5: `if (x := expr)` 是什么语法？
→ 见 [05-pattern-binding.md](./05-pattern-binding.md#基础绑定语法)

### Q6: 如何安全访问数组元素？
→ 见 [05-pattern-binding.md](./05-pattern-binding.md#最小示例)

### Q7: 为什么数组不能修改？
→ 见 [04-reference-value.md](./04-reference-value.md#不可变容器)

### Q8: 如何避免意外的对象共享？
→ 见 [04-reference-value.md](./04-reference-value.md#避免意外共享的策略)

## 🛠️ Agent 使用指南汇总

每份文档都包含 **"编程 Agent 使用指南"** 章节，提供：
- **决策树**：何时使用特定语法
- **检查清单**：代码审查要点
- **重构模式**：常见优化模式
- **最佳实践**：经验总结

**综合最佳实践**：
1. **默认不可变**：优先使用常量，最小化可变状态
2. **值优先**：默认使用 struct，仅在需要共享时用 class
3. **作用域最小化**：在最小需要的作用域声明标识符
4. **显式纯函数**：使用 `<computes>` 标记纯函数
5. **安全访问**：使用绑定语法安全访问可能失败的操作

## 📖 参考资源

所有文档基于 Epic Games 官方文档调研：
- [Verse Language Reference](https://dev.epicgames.com/documentation/en-us/fortnite/verse-language-reference)
- [Constants and Variables](https://dev.epicgames.com/documentation/en-us/fortnite/constants-and-variables-in-verse)
- [Code Blocks](https://dev.epicgames.com/documentation/en-us/fortnite/code-blocks-in-verse)
- [Failure in Verse](https://dev.epicgames.com/documentation/en-us/fortnite/failure-in-verse)
- [Specifiers and Attributes](https://dev.epicgames.com/documentation/en-us/fortnite/specifiers-and-attributes-in-verse)

## 📝 贡献与更新

这些文档是基于当前 Verse 版本的调研。如发现过时信息或需要补充内容，请：
1. 参考最新官方文档
2. 更新对应章节
3. 在文档底部注明更新日期和来源

---

**最后更新**: 2026-01-14  
**基于版本**: Verse Language Version 1.x  
**文档状态**: 初始调研完成
