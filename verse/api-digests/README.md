# Verse API Digest 文件

本目录存放从 UEFN 引擎中提取的 API Digest 文件，用于开发时查阅参考。

## 📌 重要说明

这些文件**不是项目代码的一部分**，而是从 UEFN 引擎中提取出来的 API 参考文档。

### 为什么使用 `.md` 后缀？

原始文件后缀是 `.digest.verse`，但我们在存储时添加了 `.md` 后缀：

- ✅ 方便在 GitHub 上直接查阅和搜索
- ✅ 支持语法高亮预览
- ℹ️ 实际的 API digest 文件位于 UEFN 引擎内部，不属于项目

## 📂 文件列表

| 文件 | 说明 |
|------|------|
| `Fortnite.digest.verse.md` | Fortnite API 定义 |
| `UnrealEngine.digest.verse.md` | UnrealEngine API 定义 |
| `Verse.digest.verse.md` | Verse 语言核心 API 定义 |

## 🔄 自动同步

这些文件由 GitHub Actions 工作流 [`sync-verse-api-digests.yml`](../../.github/workflows/sync-verse-api-digests.yml) 自动同步。

- **同步频率**: 每天 UTC 00:00 自动运行
- **数据来源**: [vz-creates/uefn](https://github.com/vz-creates/uefn) 仓库
- **同步路径**: `Modules/FortniteGame/{Fortnite,UnrealEngine,Verse}/`

## 🔍 如何使用

在开发 Verse 代码时，可以搜索这些文件查找：

- 可用的类、函数、属性
- API 签名和参数类型
- 模块和命名空间结构

示例：查找 `player` 相关 API

```bash
grep -i "player" verse/api-digests/*.md
```

## ⚠️ 注意事项

- 请勿手动修改这些文件，它们会被自动同步覆盖
- 如需更新，请手动触发工作流或等待每日自动同步
