# Self-Evolving Agent Framework (SEAF)

> **自进化 Agent 框架设计**  
> **版本**: 1.0.0  
> **日期**: 2026-01-02

---

## 1. 框架概述

### 1.1 目标

构建一个**自进化、循环运行**的 Agent 系统，能够：

1. **自动扩展能力边界** - 持续探索和封装 UEFN/Verse API
2. **积累可复用资产** - Skills、Wrappers、Helpers、Patterns
3. **胶水模式组装** - 真实任务通过组合现有能力快速实现
4. **自主学习改进** - 从错误中提取经验，优化 Skill 和流程

### 1.2 核心理念

```
┌─────────────────────────────────────────────────────────────┐
│                     胶水编程思想                              │
│                                                             │
│   "凡是能不写的就不写，站在巨人肩膀上"                         │
│                                                             │
│   Agent 的工作：发现 → 封装 → 组合 → 验证                      │
│   而非：设计 → 创造 → 调试 → 维护                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          Control Hub (协调中心)                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │ Task Queue  │  │ Skill Index │  │ Agent Pool  │  │ Evolution   │    │
│  │ (Beads)     │  │ (Registry)  │  │ (gh-aw)     │  │ Monitor     │    │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘    │
└─────────┼────────────────┼────────────────┼────────────────┼────────────┘
          │                │                │                │
          ▼                ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           Execution Layer                                │
│                                                                          │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐               │
│  │  Explorer    │    │  Builder     │    │  Integrator  │               │
│  │  Agent       │    │  Agent       │    │  Agent       │               │
│  │              │    │              │    │              │               │
│  │  探索新API   │    │  封装能力    │    │  组装任务    │               │
│  │  发现边界    │    │  创建Skill   │    │  胶水编程    │               │
│  └──────────────┘    └──────────────┘    └──────────────┘               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
          │                │                │
          ▼                ▼                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           Asset Layer (资产层)                           │
│                                                                          │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐               │
│  │ Skills/      │    │ Wrappers/    │    │ Patterns/    │               │
│  │              │    │              │    │              │               │
│  │ verseDev     │    │ API封装      │    │ 最佳实践     │               │
│  │ gameDev      │    │ 组件库       │    │ 模板库       │               │
│  │ githubCli    │    │ Helper库     │    │ 错误手册     │               │
│  └──────────────┘    └──────────────┘    └──────────────┘               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 组件说明

| 组件 | 职责 | 实现 |
|------|------|------|
| **Task Queue** | 任务管理、优先级、状态追踪 | Beads CLI |
| **Skill Index** | 技能注册、能力索引、依赖管理 | `Core/skills/` 目录结构 |
| **Agent Pool** | Agent 调度、并发控制、负载均衡 | gh-aw workflows |
| **Evolution Monitor** | 进化追踪、覆盖率统计、改进建议 | 自定义 dashboard |

---

## 3. 三种 Agent 角色

### 3.1 Explorer Agent (探索者)

**职责**：发现 UEFN 能力边界，探索未封装的 API

```yaml
# .github/workflows/explorer-agent.md
触发条件:
  - 定时触发 (每天)
  - 手动触发 (指定探索方向)

输入:
  - 当前 api-digests 覆盖范围
  - 已封装的 Wrapper 列表
  - 探索优先级队列

输出:
  - 新发现的 API 列表
  - 能力边界报告
  - 封装建议 (创建 Beads 任务)
```

**探索策略**：

```
1. 文档驱动探索
   └── 爬取 Epic 官方文档
   └── 对比现有 api-digests
   └── 发现未覆盖的 API

2. 代码驱动探索  
   └── 分析现有 Wrapper 依赖
   └── 寻找可组合的新能力
   └── 发现 API 间的关联

3. 错误驱动探索
   └── 分析编译错误日志
   └── 识别 API 使用模式
   └── 发现边界约束
```

### 3.2 Builder Agent (构建者)

**职责**：将探索结果封装为可复用的 Skill/Wrapper/Helper

```yaml
# .github/workflows/builder-agent.md
触发条件:
  - Beads 任务 (type: build)
  - Explorer 产出的封装建议

输入:
  - 待封装的 API 描述
  - 相关的 api-digest
  - 现有 Skill 模板

输出:
  - 新的 Wrapper 代码
  - 更新的 SKILL.md
  - 测试用例 (如适用)
  - PR 提交
```

**构建流程**：

```
1. 理解 API
   └── 读取 api-digest
   └── 分析参数和返回值
   └── 识别使用约束

2. 设计封装
   └── 确定封装层级 (Helper/Wrapper/Component)
   └── 设计接口签名
   └── 规划错误处理

3. 实现代码
   └── 生成 Verse 代码
   └── 添加文档注释
   └── 创建使用示例

4. 验证
   └── UEFN 编译检查
   └── Skill 一致性检查
   └── 更新 Skill Index
```

### 3.3 Integrator Agent (集成者)

**职责**：使用胶水编程模式，组装现有能力完成真实任务

```yaml
# .github/workflows/integrator-agent.md
触发条件:
  - Beads 任务 (type: feature/task)
  - 用户真实需求

输入:
  - 需求描述
  - Memory-bank 项目上下文
  - 可用 Skill 列表

输出:
  - 组装的解决方案
  - 使用的 Skill 依赖图
  - 缺失能力报告 (如有)
  - PR 提交
```

**胶水编程流程**：

```
1. 需求分解
   └── 将需求拆分为子任务
   └── 识别每个子任务对应的能力

2. 能力匹配
   └── 搜索 Skill Index
   └── 匹配现有 Wrapper/Helper
   └── 标记缺失能力

3. 组装方案
   └── 设计调用流程
   └── 编写胶水代码
   └── 处理接口适配

4. 缺失上报
   └── 无法匹配的能力 → 创建 Explorer 任务
   └── 需要新封装 → 创建 Builder 任务
   └── 阻塞的任务 → 标记依赖关系
```

---

## 4. 循环进化模式

### 4.1 进化循环

```
┌─────────────────────────────────────────────────────────────┐
│                      Evolution Loop                          │
│                                                              │
│    ┌──────────┐     ┌──────────┐     ┌──────────┐           │
│    │ Explore  │────▶│  Build   │────▶│ Integrate│           │
│    │          │     │          │     │          │           │
│    │ 发现能力 │     │ 封装能力 │     │ 使用能力 │           │
│    └────┬─────┘     └────┬─────┘     └────┬─────┘           │
│         │                │                │                  │
│         │                ▼                │                  │
│         │         ┌──────────┐            │                  │
│         │         │  Asset   │            │                  │
│         │         │  Layer   │◀───────────┘                  │
│         │         │          │                               │
│         │         │ 积累资产 │                               │
│         │         └────┬─────┘                               │
│         │              │                                     │
│         │              ▼                                     │
│         │         ┌──────────┐                               │
│         └────────▶│ Monitor  │                               │
│                   │          │                               │
│                   │ 追踪进化 │                               │
│                   │ 发现缺口 │                               │
│                   └──────────┘                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 Beads 任务类型

```yaml
# 进化相关任务类型
task_types:
  explore:
    description: "探索新 API 或能力边界"
    labels: ["evolution", "explore"]
    assigned_to: explorer-agent
    
  build:
    description: "封装新的 Skill/Wrapper/Helper"
    labels: ["evolution", "build"]
    assigned_to: builder-agent
    
  feature:
    description: "真实功能需求"
    labels: ["feature"]
    assigned_to: integrator-agent
    
  fix:
    description: "修复问题"
    labels: ["fix"]
    assigned_to: integrator-agent
    
  improve:
    description: "改进现有 Skill"
    labels: ["evolution", "improve"]
    assigned_to: builder-agent
```

### 4.3 任务流转规则

```
┌─────────────────────────────────────────────────────────────┐
│                    Task Flow Rules                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. 真实需求入口                                              │
│     feature → Integrator → [能力足够] → 完成                  │
│                         → [缺失能力] → 创建 explore/build     │
│                                                              │
│  2. 探索任务入口                                              │
│     explore → Explorer → [发现新API] → 创建 build             │
│                       → [无新发现] → 完成                     │
│                                                              │
│  3. 构建任务入口                                              │
│     build → Builder → [封装成功] → 解除依赖阻塞               │
│                    → [需要更多探索] → 创建 explore            │
│                                                              │
│  4. 改进任务入口                                              │
│     improve → Builder → [从错误手册学习] → 更新 Skill         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 能力覆盖追踪

### 5.1 覆盖率指标

```yaml
coverage_metrics:
  api_coverage:
    description: "已封装 API / 总 API 数量"
    target: 80%
    
  skill_coverage:
    description: "有 SKILL.md 的目录 / 总目录"
    target: 100%
    
  wrapper_coverage:
    description: "有 Wrapper 的核心 API / 核心 API 总数"
    target: 90%
    
  pattern_coverage:
    description: "有模式文档的常见场景 / 常见场景总数"
    target: 70%
```

### 5.2 进化 Dashboard

```
┌─────────────────────────────────────────────────────────────┐
│                  Evolution Dashboard                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  📊 API 覆盖率                                               │
│  ████████████████████░░░░░░░░░░  65% (156/240)               │
│                                                              │
│  📦 Wrapper 数量                                             │
│  Core: 23  |  UI: 12  |  Animation: 8  |  Audio: 5          │
│                                                              │
│  📝 Skill 完整度                                             │
│  verseDev: 17/17 ✅  |  gameDev: 8/10 ⚠️  |  githubCli: 2/2  │
│                                                              │
│  🔄 本周进化                                                  │
│  +3 Wrappers  |  +5 Helpers  |  +12 Patterns                 │
│                                                              │
│  ⏳ 待处理任务                                                │
│  explore: 5  |  build: 8  |  feature: 3                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 胶水编程实践

### 6.1 胶水代码模式

```verse
# 胶水代码示例：组装钓鱼系统

# 不创造新逻辑，只组合现有能力
fishing_game_class := class(creative_device):
    
    # 组合现有 Wrapper
    @editable ZoneWrapper: fishing_zone_wrapper = fishing_zone_wrapper{}
    @editable UIWrapper: fishing_ui_wrapper = fishing_ui_wrapper{}
    @editable RewardWrapper: reward_wrapper = reward_wrapper{}
    
    OnBegin<override>()<suspends>: void =
        # 胶水代码：连接事件
        ZoneWrapper.OnPlayerEnter.Subscribe(HandleEnter)
        ZoneWrapper.OnFishCaught.Subscribe(HandleCatch)
    
    HandleEnter(Player: player): void =
        # 调用现有 Helper
        UIWrapper.ShowFishingUI(Player)
    
    HandleCatch(Player: player, Fish: fish_data): void =
        # 组合调用
        RewardWrapper.GiveReward(Player, Fish.RewardId)
        UIWrapper.ShowCatchAnimation(Player, Fish)
```

### 6.2 能力缺失上报模板

```markdown
## 能力缺失报告

### 任务信息
- 任务 ID: {beads_task_id}
- 需求描述: {requirement}

### 缺失能力
- 能力名称: {capability_name}
- 所需 API: {api_reference}
- 预期用途: {expected_usage}

### 建议操作
- [ ] 创建 explore 任务探索此 API
- [ ] 创建 build 任务封装此能力
- [ ] 标记为外部依赖 (Epic 未提供)
```

---

## 7. 实施路线

### Phase 1: 基础设施 (当前)

- [x] Beads 任务管理
- [x] gh-aw Agent 执行
- [x] 预编译 bd 二进制
- [x] Skill 系统结构
- [ ] Explorer Agent workflow
- [ ] Builder Agent workflow
- [ ] Integrator Agent workflow

### Phase 2: 循环验证

- [ ] 单 Agent 循环测试
- [ ] 多 Agent 协作测试
- [ ] 任务流转验证
- [ ] 错误处理机制

### Phase 3: 进化运行

- [ ] 定时触发 Explorer
- [ ] 自动创建 build 任务
- [ ] 能力覆盖追踪
- [ ] Dashboard 可视化

### Phase 4: 生产应用

- [ ] 真实项目接入
- [ ] 胶水模式验证
- [ ] 性能优化
- [ ] 成本控制

---

## 8. 相关资源

- [verseAgentLoop SKILL](../skills/programming/verseDev/verseAgentLoop/SKILL.md) - 循环控制器详细设计
- [verseOrchestrator SKILL](../skills/programming/verseDev/verseOrchestrator/SKILL.md) - 协调器设计
- [胶水编程方法论](./胶水编程.md) - 核心理念
- [githubCli SKILL](../skills/programming/githubCli/SKILL.md) - GitHub CLI 操作
- [ghAw SKILL](../skills/programming/githubCli/ghAw/SKILL.md) - Agentic Workflows 详细指南
