# GitHub Agentic Workflows 触发器完整指南

> **目的**: 本文档提供完整的工作流触发器（`on:` 字段）规则，帮助 Agent 快速理解如何编写复杂的触发器配置。

## 目录

- [触发器基础](#触发器基础)
- [标准 GitHub 事件触发器](#标准-github-事件触发器)
- [Slash Command 触发器](#slash-command-触发器)
- [标签触发器简写语法](#标签触发器简写语法)
- [触发器高级配置](#触发器高级配置)
- [复合触发器模式](#复合触发器模式)
- [条件执行与过滤](#条件执行与过滤)
- [最佳实践与常见模式](#最佳实践与常见模式)

---

## 触发器基础

### 基本格式

触发器通过 `on:` 字段配置，支持三种格式：

```yaml
# 1. 简单字符串格式
on: push

# 2. 数组格式（多个事件）
on: [push, pull_request]

# 3. 对象格式（复杂配置）
on:
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened]
```

### 触发器是必需字段

每个工作流**必须**包含 `on:` 字段，否则编译会失败。

---

## 标准 GitHub 事件触发器

### Issues 事件

```yaml
on:
  issues:
    types: [opened, edited, closed, deleted, transferred, 
            pinned, unpinned, labeled, unlabeled, 
            locked, unlocked, milestoned, demilestoned,
            assigned, unassigned, reopened]
```

**常用组合:**

```yaml
# Issue 创建时
on:
  issues:
    types: [opened]

# Issue 状态变化
on:
  issues:
    types: [opened, edited, reopened, closed]
```

### Pull Request 事件

```yaml
on:
  pull_request:
    types: [opened, edited, closed, reopened, synchronize,
            converted_to_draft, ready_for_review,
            labeled, unlabeled, assigned, unassigned,
            review_requested, review_request_removed,
            auto_merge_enabled, auto_merge_disabled]
    branches: [main, develop]    # 目标分支过滤
    paths:                        # 路径过滤
      - 'src/**'
      - '*.md'
    forks: ["*"]                  # Fork 安全配置
```

#### Fork 安全配置 ⚠️

**默认行为**: `pull_request` 触发器**阻止所有 fork**，仅允许同仓库 PR。

```yaml
# 默认: 仅同仓库 PR（安全）
on:
  pull_request:
    types: [opened]

# 允许所有 fork（谨慎使用）
on:
  pull_request:
    types: [opened]
    forks: ["*"]

# 仅允许可信 fork
on:
  pull_request:
    types: [opened]
    forks:
      - "trusted-org/*"           # 组织下所有仓库
      - "trusted-user/repo"       # 特定用户的特定仓库
```

### Push 事件

```yaml
on:
  push:
    branches:
      - main
      - 'release/**'              # 通配符匹配
    branches-ignore:
      - 'feature/**'
    tags:
      - 'v*'
    tags-ignore:
      - 'v*-beta'
    paths:
      - 'src/**'
      - '!src/test/**'            # 排除路径
```

### Issue Comment 事件

```yaml
on:
  issue_comment:
    types: [created, edited, deleted]
```

**注意**: `issue_comment` 事件同时捕获 Issue 评论和 PR 评论。使用条件过滤：

```yaml
# 仅 Issue 评论
if: ${{ !github.event.issue.pull_request }}

# 仅 PR 评论
if: ${{ github.event.issue.pull_request }}
```

### Pull Request Review 事件

```yaml
on:
  pull_request_review:
    types: [submitted, edited, dismissed]

on:
  pull_request_review_comment:
    types: [created, edited, deleted]
```

### Discussion 事件

```yaml
on:
  discussion:
    types: [created, edited, deleted, answered, unanswered,
            labeled, unlabeled, locked, unlocked,
            category_changed, pinned, unpinned, transferred]

on:
  discussion_comment:
    types: [created, edited, deleted]
```

### Schedule 事件（定时触发）

```yaml
on:
  schedule:
    - cron: "0 9 * * 1"           # 每周一 9:00 UTC
    - cron: "0 0 * * *"           # 每天午夜
    - cron: "*/15 * * * *"        # 每 15 分钟
```

**Cron 语法:**

```text
┌───────────── 分钟 (0 - 59)
│ ┌───────────── 小时 (0 - 23)
│ │ ┌───────────── 日 (1 - 31)
│ │ │ ┌───────────── 月 (1 - 12)
│ │ │ │ ┌───────────── 星期 (0 - 6, 0=周日)
│ │ │ │ │
* * * * *
```

**模糊调度（推荐）:**

```yaml
# 自动分散执行时间，避免负载峰值
on:
  schedule: daily     # 每日（时间自动分散）
  schedule: weekly    # 每周（时间自动分散）
```

### Workflow Dispatch（手动触发）

```yaml
on:
  workflow_dispatch:
    inputs:
      environment:
        description: '部署环境'
        required: true
        type: choice
        options:
          - staging
          - production
      debug:
        description: '启用调试模式'
        required: false
        type: boolean
        default: false
      version:
        description: '版本号'
        required: true
        type: string
```

**输入类型:**

- `string` - 文本输入
- `choice` - 下拉选择（需配合 `options`）
- `boolean` - 布尔开关
- `number` - 数字输入
- `environment` - 环境选择器

**访问输入值:**

```markdown
部署环境: ${{ github.event.inputs.environment }}
```

### Workflow Run 事件

监控其他工作流的运行状态：

```yaml
on:
  workflow_run:
    workflows: ["CI Build", "Deploy"]   # 监控的工作流名称
    types:
      - completed
      - requested
    branches:
      - main
```

**结合条件过滤:**

```yaml
if: ${{ github.event.workflow_run.conclusion == 'failure' }}
```

### Workflow Call（可复用工作流）

被其他工作流调用：

```yaml
on:
  workflow_call:
    inputs:
      workflow_name:
        description: '调用工作流的名称'
        required: true
        type: string
      run_id:
        description: '工作流运行 ID'
        required: true
        type: string
    secrets:
      TOKEN:
        required: true
```

### 其他常用事件

```yaml
# Release 事件
on:
  release:
    types: [published, created, edited, deleted, prereleased, released]

# Label 事件
on:
  label:
    types: [created, edited, deleted]

# Milestone 事件
on:
  milestone:
    types: [created, opened, closed, edited, deleted]

# Project 事件
on:
  project:
    types: [created, updated, closed, reopened, deleted]

# Check Run 事件
on:
  check_run:
    types: [created, rerequested, completed, requested_action]

# Check Suite 事件
on:
  check_suite:
    types: [completed, requested, rerequested]

# Deployment 事件
on:
  deployment:
  deployment_status:

# Registry Package 事件
on:
  registry_package:
    types: [published, updated]
```

---

## Slash Command 触发器

### 基础语法

响应 Issue/PR 评论中的 `/command` 提及：

```yaml
on:
  slash_command:
    name: my-bot
```

这会自动响应：

- Issue 正文中的 `/my-bot`
- Issue 评论中的 `/my-bot`
- PR 正文和评论中的 `/my-bot`

### 限制触发位置

使用 `events:` 字段限制命令响应位置：

```yaml
on:
  slash_command:
    name: review
    events: [pull_request_comment, pull_request_review_comment]
```

**支持的事件标识符:**

| 标识符 | 说明 |
|--------|------|
| `issues` | Issue 正文（opened, edited, reopened） |
| `issue_comment` | 仅 Issue 评论（不包括 PR 评论） |
| `pull_request_comment` | 仅 PR 评论（不包括 Issue 评论） |
| `pull_request` | PR 正文（opened, edited, reopened） |
| `pull_request_review_comment` | PR 审阅评论 |
| `*` | 所有评论相关事件（默认） |

### 示例

```yaml
# /plan 命令 - 仅响应 Issue 和 Discussion 评论
on:
  slash_command:
    name: plan
    events: [issue_comment, discussion_comment]

# /grumpy 命令 - 仅响应 PR 相关评论
on:
  slash_command:
    name: grumpy
    events: [pull_request_comment, pull_request_review_comment]

# /scout 命令 - 响应所有位置（默认）
on:
  slash_command:
    name: scout
```

**⚠️ 注意**: `command:` 触发器已弃用，请使用 `slash_command:`。

---

## 标签触发器简写语法

### Issue 标签触发

```yaml
# 标准写法
on: issue labeled bug enhancement priority-high

# 逗号分隔写法
on: issue labeled bug, enhancement, priority-high
```

**自动展开为:**

- Issue 被添加指定标签时触发
- 自动包含 `workflow_dispatch` 触发器

### Pull Request 标签触发

```yaml
on: pull_request labeled needs-review approved ready-to-merge
```

### Discussion 标签触发

```yaml
on: discussion labeled question announcement help-wanted
```

**⚠️ 注意**: Discussion 事件不支持 GitHub Actions 的 `names` 字段过滤，标签过滤需通过作业条件处理。

---

## 触发器高级配置

### stop-after 执行截止时间

设置工作流的自动停止时间：

```yaml
on:
  schedule:
    - cron: "0 9 * * 1"
  stop-after: +1mo              # 1 个月后停止调度
```

**支持的格式:**

| 格式 | 示例 | 说明 |
|------|------|------|
| 绝对时间戳 | `"2025-06-01 00:00:00"` | 精确到秒 |
| 相对时间 | `+25h` | 25 小时后 |
| 相对时间 | `+3d` | 3 天后 |
| 相对时间 | `+1mo` | 1 个月后 |
| 组合时间 | `+1d12h` | 1 天 12 小时后 |

**注意**: 相对时间最小单位是小时(h)。

### reaction 表情符号反应

触发时自动为触发项添加表情：

```yaml
on:
  issues:
    types: [opened]
  reaction: "eyes"              # 👀
```

常用表情符号：

- `eyes` - 👀 表示正在查看
- `+1` - 👍 表示确认
- `rocket` - 🚀 表示进行中
- `heart` - ❤️ 表示喜欢

### manual-approval 手动审批

要求手动审批后才执行：

```yaml
on:
  workflow_dispatch:
  manual-approval: true
```

这会使用 GitHub Environment Protection Rules 实现审批流程。

---

## 复合触发器模式

### 多事件触发

```yaml
on:
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened, synchronize]
  workflow_dispatch:
```

### 半主动 Agent 模式

结合定时和事件触发，实现主动监控 + 被动响应：

```yaml
on:
  schedule:
    - cron: "0/10 * * * *"      # 每 10 分钟检查
  issues:
    types: [opened, edited, closed]
  issue_comment:
    types: [created, edited]
  pull_request:
    types: [opened, edited, closed]
  push:
    branches: [main]
  workflow_dispatch:
```

### CI 失败监控模式

监控特定工作流的失败：

```yaml
on:
  workflow_run:
    workflows: ["CI Build", "Tests"]
    types:
      - completed
    branches:
      - main
  stop-after: +1mo

if: ${{ github.event.workflow_run.conclusion == 'failure' }}
```

### 每日报告模式

```yaml
on:
  schedule:
    - cron: "0 9 * * 1-5"       # 工作日 9:00
  stop-after: +1mo
  workflow_dispatch: null
```

---

## 条件执行与过滤

### 使用 if 条件

```yaml
on:
  issues:
    types: [opened]

# 仅处理非 bot 创建的 Issue
if: ${{ github.actor != 'dependabot[bot]' }}
```

### 常用条件表达式

```yaml
# 仅主分支
if: ${{ github.ref == 'refs/heads/main' }}

# 排除 bot
if: ${{ !contains(github.actor, '[bot]') }}

# 特定标签
if: ${{ contains(github.event.issue.labels.*.name, 'bug') }}

# 工作流失败时
if: ${{ github.event.workflow_run.conclusion == 'failure' }}

# PR 来自 fork
if: ${{ github.event.pull_request.head.repo.fork == true }}

# 包含特定文件变更
if: ${{ contains(github.event.commits.*.modified, 'package.json') }}
```

---

## 最佳实践与常见模式

### 1. 始终包含 workflow_dispatch

允许手动触发便于测试和调试：

```yaml
on:
  issues:
    types: [opened]
  workflow_dispatch:              # 始终添加
```

### 2. 使用模糊调度避免负载峰值

```yaml
# ❌ 不推荐：固定时间
on:
  schedule:
    - cron: "0 0 * * *"           # 所有工作流同时执行

# ✅ 推荐：模糊调度
on:
  schedule: daily                  # 自动分散执行时间
```

### 3. 添加 stop-after 控制成本

```yaml
on:
  schedule:
    - cron: "0 9 * * 1"
  stop-after: +1mo                 # 1 个月后自动停止
```

### 4. Fork 安全默认

```yaml
# 默认阻止 fork，仅在必要时显式允许
on:
  pull_request:
    types: [opened]
    forks: ["trusted-org/*"]      # 仅允许可信来源
```

### 5. 事件类型精确匹配

```yaml
# ❌ 不推荐：捕获所有类型
on:
  issues:

# ✅ 推荐：明确指定类型
on:
  issues:
    types: [opened, reopened]
```

### 6. 路径过滤减少不必要触发

```yaml
on:
  push:
    branches: [main]
    paths:
      - 'src/**'                   # 仅代码变更触发
      - '!src/test/**'             # 排除测试文件
```

---

## 完整示例参考

### Issue 分类 Bot

```yaml
---
on:
  issues:
    types: [opened]
  reaction: "eyes"
permissions:
  contents: read
  issues: read
safe-outputs:
  add-labels:
    allowed: [bug, feature, documentation]
timeout-minutes: 5
---
```

### 每日团队状态报告

```yaml
---
on:
  schedule:
    - cron: "0 9 * * 1-5"
  stop-after: +1mo
  workflow_dispatch: null
permissions:
  contents: read
  issues: read
  pull-requests: read
safe-outputs:
  create-issue:
    expires: 1d
    title-prefix: "[team-status] "
timeout-minutes: 10
---
```

### 深度研究 Bot

```yaml
---
on:
  slash_command:
    name: scout
  workflow_dispatch:
    inputs:
      topic:
        description: '研究主题'
        required: true
permissions:
  contents: read
  issues: read
tools:
  web-search:
  web-fetch:
safe-outputs:
  add-comment:
    max: 1
timeout-minutes: 10
---
```

### CI 失败诊断

```yaml
---
on:
  workflow_run:
    workflows: ["CI Build"]
    types: [completed]
    branches: [main]
  stop-after: +1mo
if: ${{ github.event.workflow_run.conclusion == 'failure' }}
permissions:
  actions: read
  contents: read
safe-outputs:
  create-issue:
    title-prefix: "[CI Failure] "
timeout-minutes: 10
---
```

### 代码审查 Bot

```yaml
---
on:
  slash_command:
    name: review
    events: [pull_request_comment, pull_request_review_comment]
permissions:
  contents: read
  pull-requests: read
safe-outputs:
  add-comment:
    max: 1
  create-pull-request-review-comment:
    max: 5
timeout-minutes: 10
---
```

---

## 快速参考表

| 触发器类型 | 语法示例 | 用途 |
|-----------|---------|------|
| Issue 创建 | `on: issues: types: [opened]` | Issue 自动化 |
| PR 创建 | `on: pull_request: types: [opened]` | PR 自动化 |
| Slash 命令 | `on: slash_command: name: bot` | 聊天机器人 |
| 定时执行 | `on: schedule: - cron: "0 9 * * 1"` | 定期任务 |
| 模糊定时 | `on: schedule: daily` | 分散负载 |
| 手动触发 | `on: workflow_dispatch:` | 测试/调试 |
| 标签触发 | `on: issue labeled bug` | 标签自动化 |
| 工作流监控 | `on: workflow_run: workflows: [CI]` | CI 失败响应 |
| 复用工作流 | `on: workflow_call:` | 模块化工作流 |

---

## 相关文档

- [官方指引.md](官方指引.md) - 完整 Frontmatter Schema
- [WORKFLOW-INDEX.md](WORKFLOW-INDEX.md) - 工作流模板索引
- [CAPABILITY-BOUNDARIES.md](CAPABILITY-BOUNDARIES.md) - 能力边界
