---
name: verse-audit-dispatcher
description: 审计子调度器 - 意图解析、深度/范围选择、强制阻断检测、调度具体审计Skill
version: 1.0.0
---

# Verse Audit Dispatcher

> **类型**: 子调度器（Sub-Dispatcher）  
> **职责**: 审计意图解析、参数配置、强制阻断检测、调度执行层

---

## When to Use This Skill

由 `verse-orchestrator` 在审计模式下自动调度，不应被用户直接调用。

---

## 审计类型

### 代码审计 (Code Audit)

**执行 Skill**: `verse-code-auditor`

**检查依据**:
- `shared/checklists/code-quality-checklist.md`
- `shared/checklists/architecture-compliance-checklist.md`

**触发关键词**:
- "审计代码"
- "检查代码质量"
- "审计代码库"

---

### API 一致性审计 (API Audit) ⚠️ 新增

**执行 Skill**: `verse-code-auditor` (使用 API 规则集)

**检查依据**:
- `shared/api-digests/Fortnite.digest.verse`
- `shared/api-digests/Verse.digest.verse`
- `shared/api-digests/UnrealEngine.digest.verse`
- `verse-code-auditor/SKILL.md` 中的 API-001/002/003 规则

**检查内容**:
| 规则 | 描述 | 严重度 |
|------|------|--------|
| API-001 | 类型匹配（int vs float） | 🔴 Error |
| API-002 | 接口直接实现检测（如 `fort_character` 无需 `GetXXX[]`） | 🔴 Error |
| API-003 | 废弃 API 检测 | 🟡 Warning |

**触发关键词**:
- "审计 API 调用"
- "检查 API 一致性"
- "digest 校验"

**自动触发条件**:
- verse-digest-sync 检测到上游 API 更新

---

### Prompt 审计 (Prompt Audit)

**执行 Skill**: `verse-prompt-auditor`

**检查依据**:
- `shared/checklists/prompt-quality-checklist.md`

**触发关键词**:
- "审计 Skill"
- "检查 Prompt"
- "审计 Skill Prompt"

---

## 审计深度

| 深度 | 说明 | 预计时间 | 适用场景 |
|------|------|----------|----------|
| **快速** | 仅检查索引和结构 | ~2分钟 | 日常快速检查 |
| **标准** | 抽样检查每类文件 | ~5分钟 | 定期审计 |
| **深度** | 逐文件逐行审查 | ~15分钟 | 重大变更后 |

---

## 审计范围

### 代码审计范围

| 范围 | 说明 |
|------|------|
| **全部** | 遍历 `code-library/` 下所有文件 |
| **指定类别** | 仅审计 Helpers/Components/Events/Entities 中某类 |
| **指定文件** | 仅审计用户指定的具体文件 |

### Prompt 审计范围

| 范围 | 说明 |
|------|------|
| **自动（全部）** | 审计所有 Skill (L1-L5 + orchestrator + auditors) |
| **指定模式** | 仅审计用户指定的 Skill |

---

## 调度流程

```
接收审计请求
    │
    ├── 解析审计类型
    │   ├── 代码审计 → 准备调度 verse-code-auditor
    │   ├── API审计 → 准备调度 verse-code-auditor (API规则集)
    │   ├── Prompt审计 → 准备调度 verse-prompt-auditor
    │   └── 多类型 → 依次调度
    │
    ├── 询问审计深度
    │   "请选择审计深度：快速 / 标准 / 深度"
    │
    ├── 询问审计范围
    │   ├── 代码: "审计全部代码 / 指定类别 / 指定文件？"
    │   └── Prompt: "审计全部 Skill / 指定 Skill？"
    │
    ├── 构建调度参数
    │   {
    │     type: "code" | "prompt",
    │     depth: "quick" | "standard" | "deep",
    │     scope: "all" | string[]
    │   }
    │
    ├── 调度执行层
    │   └── 传递参数给具体 auditor
    │
    └── 汇总报告
        └── 输出到 @audit-report.md
```

---

## 强制审计阻断

### 阻断条件

以下条件触发时，**必须完成审计才能继续编码**：

| 条件 | 严重程度 | 检测方式 |
|------|----------|----------|
| 跨层依赖反转 | 🔴 阻断 | Component import Entity |
| Component 直接调用 UEFN API | 🔴 阻断 | 检测 `fort_character.` 等直接调用 |
| 事件流向违规 | 🔴 阻断 | 子组件使用 `SendDown` |

### 阻断流程

```
orchestrator 启动
    │
    ├── 读取最近修改的代码文件
    │
    ├── 快速扫描阻断条件
    │   ├── 发现阻断条件
    │   │   ↓
    │   │   "⚠️ 检测到严重框架违规！
    │   │    问题: [具体问题]
    │   │    必须完成审计后才能继续编码。
    │   │    正在启动强制审计..."
    │   │   ↓
    │   │   调度 verse-code-auditor (深度模式)
    │   │   ↓
    │   │   用户确认修复
    │   │   ↓
    │   │   解除阻断
    │   │
    │   └── 未发现 → 正常执行
```

### 非阻断提醒

| 条件 | 严重程度 | 行为 |
|------|----------|------|
| 代码库文件数 +10 | 🟡 提醒 | "建议进行代码审计" |
| 命名规范不一致 | 🔵 建议 | 仅记录，不阻断 |
| 缺少文档注释 | 🔵 建议 | 仅记录，不阻断 |

---

## 参数传递格式

向执行层 Skill 传递参数：

```markdown
---
audit_type: code | api | prompt
audit_depth: quick | standard | deep
audit_scope: all | [具体列表]
forced: true | false
digest_check: true | false  # API审计时启用digest校验
---
```

---

## 输出汇总

审计完成后，汇总各执行层的报告：

```markdown
# 审计报告汇总

**审计时间**: 2025-12-27 14:30
**审计类型**: 代码审计 + Prompt审计
**审计深度**: 标准
**审计范围**: 全部

## 代码审计结果
[来自 verse-code-auditor 的报告]

## Prompt审计结果
[来自 verse-prompt-auditor 的报告]

## 综合建议
[dispatcher 汇总的改进建议]
```

---

## Reference Files

- [verse-orchestrator](../verse-orchestrator/SKILL.md) - 主协调器
- [verse-code-auditor](../verse-code-auditor/SKILL.md) - 代码审计执行层
- [verse-prompt-auditor](../verse-prompt-auditor/SKILL.md) - Prompt审计执行层
- [verse-digest-sync](../verse-digest-sync/SKILL.md) - API 上游同步检测
- [shared/checklists/](../shared/checklists/) - 检查清单
- [shared/api-digests/](../shared/api-digests/) - 官方 API 定义

---

*最后更新: 2025-12-27*
